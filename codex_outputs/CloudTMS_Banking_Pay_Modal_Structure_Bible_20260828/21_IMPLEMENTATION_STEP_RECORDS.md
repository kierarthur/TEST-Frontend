# Bounded implementation step records

These records consolidate the tool execution and scoped reviews. A PASS below applies only to its stated component purpose. It is not a PASS for an entire BP requirement, phase, deployment or zero-loss acceptance. Outstanding integrated requirements remain IN_PROGRESS or NOT_STARTED.

## Shared authority and preserved baseline

- User authority: full TEST-only implementation, testing and eventual push/deployment; keep the Bible current; no production or payment/provider execution.
- Frontend base: `b66b2a61d3d266ef4e9a3d410061274ca74a7a8e`; original `js/main.js` remains unchanged.
- Backend current base: `272eea49af6e26123c867be15cde072b967c91e8`; work remained uncommitted while the isolated worktree fast-forwarded through the other task's Rota commits.
- Working trees: task-owned `banking-pay-frontend-20260828` and `banking-pay-backend-20260828`; original dirty clones and other tasks' containers were not edited.
- Current frontend AGENTS SHA-256: `12d2db00e94fae90c4a324f7aafaa532349f24f7d2938b6fd3076cbf510a24fd`.
- Current backend AGENTS SHA-256: `3c03776c33e8ce6ce713cc676356ed5d462f1ed643fbdf60d6b81dd803dc37bf`.
- Upstream Banking Pay Bible SHA-256: `66546fc546519a38bb485496fbc77ae16071e5d099fd2f436bd172129ba7d218`.
- Canonical audit SHA-256: `afd124655e4020fa061209e23914bb25d853f05bb6546dfb9daa718f76206d6f`.
- Database tests: synthetic disposable PG17 at loopback 55441 only. Installed Miget Banking definitions and deployed application remain unchanged.
- Implementer and technical reviewer: Codex; these are self-reviews, not independent-agent approval.

## STEP-004 — case-resolution presentation guards

- Requirements: BP-080/081/082/087/089. Existing controls: openBucketedResolution, openNonBucketResolution, openTaxableFinanceCaseRestructure, toggleExcludeTimesheet.
- Files: backend `28082026_1354_banking_pay_modal_case_action_facts.sql`, v2 include closure, case-action tests and frozen legacy oracle.
- Purpose: publish exactly the current case button/metadata facts without saving, cancelling or recalculating any resolution.
- Pre-change proof: missing new SQL failed the source test. The oracle captures the complete unchanged case normalization/display-overlay/button functions. Added edge cases exposed whitespace-flag and unmatched nested-case differences; both were corrected before dependent use.
- Scoped results: four source/runtime tests pass, covering 865 fixtures in real local PG17. No illegal schema-qualified conditional functions, application data writes, browser/service grants or SECURITY DEFINER helper changes are introduced.
- Wider result: frontend guards still prove all 52 frozen declarations are unchanged. Final full backend/release replay remains required.
- Performance: bounded pure metadata work only; full deduplicated Action query and representative timing are not yet proved.
- Zero-loss limit: current handlers remain reachable in the unchanged legacy UI. New task deduplication and UI/action request parity are not complete.
- Evidence: EVIDENCE-0009. Result: PASS for presentation characterization only. Next dependent classifier work may use these tested facts; UI activation/release is NOT authorised by this record.

## STEP-005 — compact candidate-table presenter

- Requirements: BP-020–026, BP-029–037, BP-016 (component part). Copy: MSG-006/008/010/011/012/014/017/018/019/020.
- Files: `js/banking-pay-modal-v2-table.js`, scoped `css/banking-pay-modal-v2.css`, unit tests and synthetic browser fixture/verifier.
- Purpose: a four-column, non-expanding, server-fed 100-candidate table with one delegated interaction owner and stable row nodes.
- Pre-change proof: no v2 table existed; the original integration tests were red and remain red until the full orchestrator/action bridge is implemented. New renderer tests describe the controlling four-cell and selected-only contract.
- Scoped results: 20 unit tests and ten browser verification groups pass. Currency formatting uses strings only; it retains the current pound-sign/negative-sign order and all decimal digits. It never sums, rounds, sorts, filters or reclassifies payments.
- Browser result: 100 rows, four cells each, common 43px height at the measured normal viewport; narrow host 423px versus 760px table uses horizontal scrolling, not wrapping. Largest supported decimal remains intact. Twenty mount/unmount cycles leave one table and one event dispatch.
- Shared copy: accepted messages come from the existing v2 copy module. No independent reason mapper was added.
- Scope limit: the browser fixture is explicitly synthetic and has a network-denying policy. No real Workbench or Draft endpoint is available through it. Complete modal/zoom/keyboard/memory/latency tests remain open.
- Mechanism detail: a small table helper is composed by the eventual single v2 orchestrator; it has no independent API/session/selection authority. See DEV-0002.
- Evidence: EVIDENCE-0010/0011. Result: PASS for component presentation only. Main integration remains disabled.

## STEP-006 — serialized update settlement

- Requirements: BP-007/015/016/026/033/051/053/077 (applicable settlement portions; final row-specific parity remains required). Export, resolved receipts and actual Draft input are not tested by this component.
- Files: `js/banking-pay-modal-v2-settlement.js` and its unit tests.
- Purpose: one queue and one accepted graph; existing action adapters must provide server results, same-revision reconciliation and authoritative read-back.
- Scoped results: 20 tests pass. They include duplicate submission prevention, updated revision at queued dispatch, one synchronous adoption boundary, no optimistic amounts, stale rejection read-back, lost-response read-back without a mutation retry, invalid Ready/Blocked graph rejection, closure, late reads and visible callback failure.
- Existing server authority: unchanged. The module performs no actual network call or financial calculation. Mutation execution and response identity validation remain the responsibility of the not-yet-connected existing-action adapters.
- Important open proof: full adapter response-to-reconciliation revision binding, alias replacement, dynamic-sort anchored page renewal, all 45 real action entry points and actual recovery movement remain required before acceptance.
- No fallback after a mutation is dispatched. Closing aborts local reads only and does not pretend a submitted mutation was cancelled.
- Evidence: EVIDENCE-0012. Result: PASS for the tested state-machine component. No complete selection/phase/release claim.
- Follow-up hardening: added failing tests for a bare success, wrong intent/session/scope, stale/untyped receipt, and a reconciliation read newer than the accepted mutation. Existing-action adapters must return a normalised, validated receipt carrying request ID and exact session/version/progress/scope. Unproved success takes read-back, not optimistic adoption or mutation retry. Reconciliation is pinned to that exact receipt; same-version read-back cannot silently change filter scope. The original 20 tests remain, with nine added negatives.
- Follow-up result: all 29 settlement tests pass. EVIDENCE-0018. Navigation/read adoption and integration with actual action receipts remain open.
- Navigation follow-up: all 37 settlement tests now pass. Accepted page/detail navigation becomes the graph used by the next mutation. Later navigation wins even at the same financial revision; a mutation or close invalidates pending reads; a newer financial revision requires full read-back rather than a partial page adoption. Invalid child pages do not publish. This closes the component navigation gap only, not the actual legacy-action adapters. EVIDENCE-0021.

## STEP-007 — candidate selection through the current selection owner (IN PROGRESS)

- Requirements: BP-007/026/027/028/033/053/070/077/123/131/132/139/140/141. Fixtures: SEL-007–013/017–024 and REC-005–009. Same shared baselines/instruction hashes above.
- Planned files: additive `28082026_1424_banking_pay_modal_candidate_selection_core.sql`, exact legacy-owner bridge repeatable, source/rollback-runtime tests and one mechanical bridge generator.
- Mechanism: the existing `pay_workbench_session_set_selected_rows` owns dispatch. An explicit, strictly validated v2 candidate-intent branch calls a private helper; all pre-existing request branches remain byte-for-byte unchanged. The private helper calls the existing recovery revalidator once and does not calculate money/headroom or create financial artefacts. Its result is internal until the complete public summary/mutation envelope is implemented.
- Lock order: session, candidate scope, scoped current rows. Complete candidate membership is derived on the server. Only final eligible Ready rows receive the explicit candidate override; blocked rows are changed only by the existing recovery owner when eligibility actually changes.
- Duplicate handling: one compact last-request receipt in session progress; a matching immediate duplicate returns that accepted receipt without writing. A later revision must return a stale/read-back response, never replay the old command. Reusing the same retained request ID with changed input is a typed conflict. No automatic mutation retry is introduced.
- Genuine no-op: if every current eligible Ready row already has the requested explicit selection override, no row, progress revision, audit or receipt is written. A changed intent with an unchanged visible tick is still a real audited change.
- Compatibility proof required before dependent use: all old function-body bytes except the guarded dispatch insertion unchanged; old request fixtures unchanged; candidate all-page scope, one recovery call/revision/audit, no-op/duplicate/stale/rollback/isolation proved on local PG17. No v2 activation/publication is permitted while these gates are open.
- Component results: seven source-equivalence/security tests pass. Three rollback-contained real PG17 scripts pass: channel isolation, candidate selection lifecycle, and malformed/stale/hidden-row negatives. A 105-payment candidate selects 106 final Ready rows after its existing £15 recovery becomes eligible, with the expected £1,035 display amount; clearing produces NONE and £0. The original individual and global selection branches remain operational in the fixtures.
- Runtime checks prove one revision/audit/revalidator, immediate duplicate receipt replay without another write, a genuine no-op without any write, conflicting/reused/stale request rejection, and complete rollback when the recovery owner fails. Other-channel and other-candidate rows remain byte-identical under the scoped operation.
- Limits: the public candidate response, complete global/Draft/section facts, bounded movement/receipt payload, current-session V3 compatibility, representative performance and full browser integration remain open. This is not completion of BP-027/028 or a release gate. Evidence: EVIDENCE-0013/0014/0015.
- Source finding before implementation: the current V3 recovery helper partitions calculations by channel, but its UPDATE writes both channels. The scoped v2 intent therefore needs an optional write-channel constraint in that same helper, reached through a three-argument overload of the existing recovery owner. The original two-argument caller and all calculation text stay unchanged. The overload rejects a scoped non-V3 request before writes instead of applying the legacy unpartitioned branch. Source equivalence, existing ALL behaviour and exact other-channel row preservation must be proved; this is a D1 technical mechanism, not a change to deduction allocation. Dependent/public activation remains stopped until its equivalence tests pass.

## STEP-008 — component action and issue-list preservation (IN PROGRESS)

- Requirements: BP-080–089/099/100/102/110/121/123. Existing component actions remain suggested value, manual rate, manual amount and clear. No action is newly authorised by a presentation flag.
- First bounded change: freeze the complete unchanged component renderer and compare its exact button guards against a private read-only projection. The current published component flags, including strict-boolean versus truthy behaviour, must be retained; a fixed/reimbursement flag must not silently remove a control that currently exists.
- Keep required actions separate from optional clear/exclude/snooze controls when classifying Action Required. An optional button alone does not make a passive payment a human-action task.
- A second query-contract omission found in the first Worker draft is BP-121: Blocked also retains list search/sort/paging. Its inputs must be scoped to the Blocked list and must never modify Ready selection or become main-table search. Exact list columns/query keys and same-scope cursor tests remain required before activation.
- Component result: four tests pass, including 1,280 button-state combinations and 100 truthy/nested input cases against the complete unchanged legacy renderer. The frozen oracle now contains 53 complete declarations. EVIDENCE-0016. Complete issue-list ownership and UI execution remain in progress; no whole-requirement PASS or public activation.
- List follow-up: the Worker accepts explicit BP-102/BP-121 list-only search/sort and validates echoed query/count context; 82 Worker tests pass. A bounded Action/Blocked presenter has 29 passing unit checks. Exact columns/keys/bounds are in document 17. SQL list/cursor/classification and real browser/action execution remain open. EVIDENCE-0022/0023.

## STEP-009 — reuse the current Draft readiness reader (IN PROGRESS)

- Requirements: BP-007/130/131/132/139. No Draft execution is part of this step.
- Fresh permanent Miget read-only connector proof confirms agency_test healthy, all nine fixed tools visible, and the complete current progress-counter owner body equal to the repository after line-ending normalisation. The compact-progress helper was also retrieved and inspected. No hosted function was executed or changed.
- The private v2 display gate will call the existing counter owner exactly once with `p_apply=false` and `p_write_progress_json=false`. It must retain all current blocker codes and only narrow the gate when no currently selected Ready payment remains inside the active filters. It must not reimplement source/job/publication readiness rules or construct a Draft.
- This is the server display gate only. Existing frontend first-page/review, same-week PAYE, permission, password/2FA and actual Draft mutation validation remain with their current owners and require separate integration tests.
- Result: two source checks and 34 rollback-contained real PG17 state checks pass, including pending/failed/stale-source/zero-selection and typed invalid input. Session, scope, rows and jobs remain byte-identical after each read. EVIDENCE-0017. Full Draft manifest equivalence, frontend guard integration and performance remain open.

## STEP-010 — bounded reuse of existing detail renderers (IN PROGRESS)

- Requirements: BP-050–078 and existing component/case detail controls. The unchanged legacy view remains the comparison and fallback owner.
- Extract complete existing pure rendering declarations mechanically into a contained detail factory. Resolve lexical dependencies from the JavaScript syntax tree, not indentation or text matching; nested variable names must not become bogus context inputs. Supply only bounded, accepted page/context facts.
- Preserve existing payment-detail presentation and action data attributes byte-for-byte before applying the separately approved copy changes. Do not use the full legacy modal render, its whole-graph cloning or its state-writing preparation as a child renderer. No new headline/selection/financial calculation is introduced.
- Prove source provenance, exact extracted bodies, explicit external dependencies, action attributes and representative full HTML parity. Group actions that need unloaded rows require complete server scope metadata before integration; a loaded child page must never silently become the full selection/export/snooze scope.
- This step is not main activation or completed action execution. Legacy request adapters, all-page scope and browser lifecycle proof remain separate gates.
- Mechanical extraction result: 115 complete legacy declarations (111 functions and four constants), with 21 explicitly supplied context inputs. The generator resolves JavaScript lexical scope, checks source hashes and reproduces the unchanged bodies. Four syntax tests and eight full-renderer parity/provenance tests pass; the original main asset is unchanged. EVIDENCE-0019.
- Added a Candidate Banking presenter which accepts one bounded candidate Ready page at the same revision as the summary. It reuses those renderers, emits existing action attributes, keeps expansion local, restores checkbox focus/scroll and prepares the replacement before publication. It has no network or financial authority. Four focused tests pass. EVIDENCE-0020.
- Twelve browser component checks pass on synthetic records, including half-ticks, exactly one explicit intent, non-optimistic state, expansion, accepted updates, focus restoration, retained main row, invalid-page rejection, busy controls, close restoration and twenty reopen cycles. These are isolated component checks, not deployed application or performance acceptance. A working Candidate detail screenshot was shared with the user. No real account/payment data or services were involved.
- Supplemental action finding: the current grouped Timesheet renderer emits `banking:finance:clearTimesheetSnooze`; its handler is outside the original pay-namespace ledger. The adjacent `banking:finance:clearAllTimesheetExpenses` alias shares the existing all-expenses handler. Both are added to supplemental preservation checks without deleting or renumbering the original 45 actions. The all-expenses action must retain its fresh authoritative full expense-set resolver, including off-page rows; loaded-page counts cannot substitute for it.
- Remaining detail gates: exact modal-framework/delegated-handler adapter, complete off-page group/expense metadata, all-page CSV/Timesheet selection, approved copy substitution, and real revision/polling integration. No main-table expansion was added.

## STEP-011 — Create Draft contract preservation (IN PROGRESS)

- Requirement: the user's explicit critical 28 August instruction; BP-007/130/131/132/139 and all affected filter/selection/action paths. This is a release-blocking invariant, not a change in product behaviour.
- The original frontend `bankingPayCreateDraft` and Worker `handleBankingPayCreateDraft` remain unchanged and now have full-owner source-hash guards. Three fresh installed Miget definitions match the local originals exactly after line-ending normalization.
- Added a small immutable review-snapshot presenter using the existing `selected_set_complete=false` plus exact revision contract. It never invents a complete list, readiness flag, first-page approval or request body. Atomic application adoption is still to be connected.
- Frontend result: 31 new owner/request-projection comparisons pass; 73 tests pass with the existing shared-selection/recovery/TS_TOTAL/Draft-safety checks. EVIDENCE-0024.
- Cross-system result: 17 tests pass with no skips. Two rollback-only PostgreSQL flows compare old individual selection versus new candidate selection, then execute unchanged Worker and frontend Draft readers on the resulting real database payloads. Complete 111/110/2 selected counts and PAYE/Umbrella subsets match. EVIDENCE-0025.
- The real final database lock guard accepts the exact reviewed set through to a deliberately installed local refresh blocker; omitted payment, candidate-ID substitution, duplicate and stale revision are rejected before that blocker. Thirty such guard checks pass; no batch/items are created and no synthetic operation/job survives the subtransaction. All fixture data is rolled back.
- Fresh installed-source evidence: EVIDENCE-0026. Exact hashes, field mapping, limits, fixture correction and remaining gates are in `23_CREATE_DRAFT_COMPATIBILITY_CONTRACT.md`.
- Still open: actual controller/legacy alias/button integration, full preflight/final browser request equality, every moved action before Draft, concurrency/transport lifecycle, full economic/frozen-scope cases, deployed API/browser and performance proof. No Draft execution or v2 activation claim.

## STEP-012 — complete candidate authority in paged Ready details (COMPONENT PASS)

- Requirements: BP-007/016/027/033/130; candidate Ready route/row envelope, fixture CB-001/CB-003/PAGE-014. Current source and instruction baseline remains that recorded for STEP-011. Original financial, selection and Draft owners remain unchanged.
- Added the complete canonical candidate row to each child response. It covers all Ready pages, remains available when the candidate moves off the main page, and is null only when no selectable candidate remains. Supplied aliases must agree before adoption.
- Local rollback proof uses 105 Ready payments and two pages, including zero selection, filtered/hidden/context-only candidates, selected Timesheets and stale revision checks. At this patch boundary Worker91, Candidate/detail19, browser13 and unchanged Draft/selection/projection28 checks passed. EVIDENCE-0027.
- Performance scope: still one Ready request. Complete representative query/Worker/browser timing is open; no speed acceptance is inferred from synthetic checks. Actual controller and all-action integration remain gated.

## STEP-013 — current sorted-page renewal and Previous navigation (COMPONENT PASS)

- Requirements: BP-016/021/022/025/026/027/033; existing anchored reconciliation request budget and server-before-pagination sorting. No product or wording change.
- Files: pending structure repeatable and paging rollback fixture; Worker summary validation; frontend table validation/presenter and explicit synthetic fixtures/tests.
- Added read-position anchors distinct from revision-bound ordinary cursors. Same session/version/scope/sort/page-size binding is mandatory; current data is reread. Anchors do not supply financial or mutation authority. The server returns aligned page number, Previous cursor and current anchor. The table uses those fields, not a local page counter.
- Pre-change proof: the paging fixture exposed missing anchored renewal/Previous; nine Worker and eight frontend negative tests failed for missing/contradictory paging metadata before the boundary checks were added.
- Runtime proof: 105 candidates; all three sorts in both directions; duplicate-name/reference ties; unchanged and changed revisions; stale normal cursors; changed scope/session/version/sort/direction/page size and future revisions; candidate removal; amount-driven movement to the final page; removal of that final page; true empty result; exact Previous/Next round trips. All fixture mutations roll back in the isolated local PG17 database.
- Boundary results: 100 Worker tests and 76 table/Candidate/settlement tests pass. EVIDENCE-0028. Browser/controller integration and query-plan/representative latency checks are not yet accepted. Capability remains false, with no hosted mutation, push or deployment.

## STEP-014 — Action/Blocked browser component lifecycle (COMPONENT PASS)

- Requirements: BP-097/099/100/102/110/117/121 and shared loading/failure/focus/teardown gates. Existing approved MSG-030/044/045/067/071/090 text is used in explicitly synthetic fixtures, not fabricated customer data. Source and instruction baseline remains unchanged; no financial/selection/provider owner changes.
- Added an exact-file, loopback-only issue-list browser fixture. Its policy denies all API connections. It exercises the actual list presenter with synthetic deduplicated-task counts and passive Blocked rows.
- A real browser regression first failed: accepting a new page while busy lost the remembered disabled-control set, leaving persistent search/sort inputs disabled after success. Replaced that remembered set with direct derivation from busy state plus current accepted page navigation. No network or authority was added to the presenter.
- Also tightened Blocked candidate UUID validation and suppressed the second click of a double-click on a list button. Existing exact detail keys and list-only search/sort intents remain unchanged.
- Both component browser lifecycles now pass: three columns; no selection controls; one detail/sort/search intent; accepted-update controls restored; end-of-list controls preserved; invalid revision changes no DOM; focus restoration; visible failure; filtered empty state; no payment-headline change; separate Updating; narrow horizontal overflow; teardown; twenty reopen cycles with one handler. Thirty issue unit tests pass.
- A distinct Action Required component screenshot was shared with the user, explicitly marked synthetic/unintegrated. Numeric column headers were subsequently aligned with their values and fixed fixture rows placed in the declared alphabetical order. No claim of real-server classification, complete modal-framework integration, hosted browser testing or speed acceptance. EVIDENCE-0029.

## STEP-015 — first-summary scope discovery (BOUNDARY PASS)

- Requirements: BP-001/007/016 and existing filter/Draft scope; first-open request budget. The initial API draft required a hash the browser had not yet received. It must not invent that hash or add another context call.
- The first summary request may omit scope hash only when it also has no cursor. Expected session/progress versions and current channel remain mandatory. The private summary-context adapter derives the hash from the existing session then calls the unchanged strict context validator. Every other read/mutation retains a mandatory exact hash. No new financial, selection, session or filter authority.
- New transport tests first failed because the initial request was rejected. Worker103 now passes, including absent/invalid response hashes, prohibited unbound continuation/detail, unchanged exact one-RPC behavior and all existing negative tests.
- Rollback-contained local PG17 proof compares initial discovery directly with the strict existing context and rejects stale revision, wrong supplied scope, unauthorised actor and cursor-without-scope. The full Candidate Ready runtime fixture also passes. EVIDENCE-0030 / DEV-0004.
- Public summary assembly and actual first-open integration remain open. This adapter is private, not a new callable browser RPC. No hosted mutation or activation.

## STEP-016 — complete current source-progress facts (COMPONENT PASS)

- Requirements: BP-007/009/010/011/041/096/097/098. Shared authority, worktree preservation and baseline above remain unchanged. The current Miget `agency_test` progress-owner definition was freshly retrieved through the permanent read-only connector; its complete 45,234-character trimmed body is byte-identical to the repository body.
- Characterization: the old progress owner returns bounded diagnostic samples. Those samples cannot become the complete Updating/Action list. Its exact current publication and pending-job ownership queries were inspected and mechanically extracted, including valid successor-job checks, into a new private read-only projection. The original owner is not edited.
- Files: source-progress generator, `28082026_1650_banking_pay_modal_source_progress_facts.sql`, rollback fixture `28082026_1652_banking_pay_source_progress_runtime.sql` and source/runtime tests.
- Four tests pass with no skips, including 26 real old/new progress comparisons over all 105 synthetic scope candidates. Ready, failed, dirty and publication states; missing owner; terminal/wrong-candidate/stale-version/stale-source/wrong-type/malformed-build owners; and valid/invalid replacement jobs are covered. Version/session negatives fail typed. The installed nondeferrable missing-owner FK is tested, not disabled to create an impossible state.
- The first parity fixture incorrectly expected a field omitted by the existing compact response. It now compares the owner's retained Draft blocker for publication incompleteness. A second fixture correctly uses the counter already created by the candidate trigger. Neither correction changes application logic or weakens the old owner.
- Every read is compared before/after for session, scope, preview and job equality; all fixture state rolls back. No repair, job submission, Draft or provider action occurs. The source helper is not a task classifier, does not grant service/browser access and does not activate v2.
- Evidence: EVIDENCE-0031. Complete task grouping, bank/readiness states, current human actions, all-source integration and representative latency remain open. A stuck source must not become Updating merely because its persisted status says pending.

## STEP-017 — preserve synthetic bank-readiness presentations (COMPONENT PASS)

- Requirements: BP-090/091/092/093/094/095/096/097/100. Current main source remains byte-identical. This stage retains the old synthetic bank row for a bank issue that has no physical Blocked preview row; otherwise a straightforward table move would omit it.
- Files: `28082026_1657_banking_pay_modal_payee_readiness_projection.sql`, four source/runtime tests, and five additional exact declarations in the frozen legacy oracle. The source generator now captures 58 declarations; the original main source hash remains `eaa48859cd2b2d9fb4bc4ed33727ab724d4d05041695712e2cbaa18f89e76bae`.
- Characterization uses the complete unchanged `collectPayeeReadinessBlockedLines` and route helper. Real local PostgreSQL results equal its full payload over 256 blocker/job combinations, 18 alias/amount/false/missing-row fixtures and 14 route/fallback cases. The old display-route fallback is explicitly not a financial key or bank-action authority. Exact bank-action hash semantics remain distinct.
- A parity test caught use of the line helper's duplicate-removing blocker parser; the synthetic-row parser now preserves the original array and order exactly. No financial equation, bank details, provider submission or selection is added.
- Wider bank/case/component/readiness suites: 15 tests pass with no skips, retaining the earlier 1,537 bank, 865 case and 1,380 component comparisons. Original action handlers remain untouched.
- Evidence: EVIDENCE-0032. This proves individual source projections, not the complete issue list: full scoped source collection, shared-account task deduplication, affected-payment membership, current job/result generations, exact action/detail integration, indefinite-snooze exclusion and query performance remain publication gates.

## STEP-018 — complete scoped bank-source collection (COMPONENT PASS)

- Requirements: BP-090/095/097/099/100/121 and indefinite-snooze exclusion; shared baseline and exact existing row eligibility, source progress and bank-payload evidence above apply. This work changes no public endpoint, financial owner, selection owner or handler.
- Planned files: one private read-only bank-source collection repeatable and rollback/runtime/source tests. It will combine existing eligible physical rows with current stored payee fragments, retain every affected candidate, and retain original payloads for later detail/action grouping. Physical sources and stored aliases must not duplicate the same candidate/account record; different candidates sharing an umbrella must never be dropped as duplicates.
- Required proof before dependent use: complete multi-candidate fixture beyond one page; physical and synthetic sources; precedence and explicit false; scoped filters and current source/version; indefinite snoozes; no state writes; no provider/bank calls; bounded set-wise query shape. Task-level deduplication and final Action/Updating/Blocked classification are separate unfinished requirements.
- Initial source/runtime tests now pass for 105 shared-owner candidates. Five local collection timings were 1025/971/742/699/751 ms; this is a synthetic query observation, not acceptable proof of the final speed target. Before dependent use, remove identical raw alias copies before expensive projection and return non-problem rows before formatting unused bank metadata. Re-run full payload equality and collection fences after this no-contract-change optimization.
- Follow-up: identical aliases are removed before projection and non-problem rows return early. The complete existing candidate bank-metadata normalization is frozen separately and compared against 21 fixtures, including nested payee context. The source collector uses that projection, so a stored nested bank owner is not silently lost.
- Eight bank-source/readiness tests now pass with no skips. The rollback fixture covers 105 candidates sharing one bank owner, physical-source precedence, duplicate aliases, all channel scopes, Candidate/Client filters, indefinite-only hidden candidates, outdated source/version, nested metadata and mismatched source identity. No per-candidate database query or application write is introduced.
- Reproducible isolated-loopback timings after both optimizations and metadata normalization: 936.82/621.97/564.73/603.32/559.95 ms (median 603.32 ms). This is a synthetic source-collection measurement, not a complete API/browser performance pass. The three fixed eligibility scans and representative full-query latency remain review gates.
- Evidence: EVIDENCE-0033. Final human-task grouping, provider/environment/result-version authority, affected-payment membership, complete detail amount fallback and issue-list integration remain open. No public endpoint or v2 activation is enabled. Database source integrity passes with 201 migrations and 424 repeatables.

## STEP-019 — selected-Timesheet shortcut after another candidate changes (COMPONENT PASS)

- Requirements: BP-007/027/034/035 and the one-mutation Candidate-sort request budget. No change to selected-only Timesheet membership or the Draft contract.
- Identified integration gap: the compact mutation response updates the changed candidate and the session revision. Other visible candidates can retain an older opaque Timesheet token even when their exact selected Timesheets are unchanged. An extra full-summary read after every tick would violate the accepted fast path.
- Proposed D1 read-only mechanism: require the request's exact current session/version/filter context as before; accept an older token revision only within that same session version, candidate and scope, never a future/missing/invalid revision; recompute the complete current selected Ready Timesheet IDs and require their hash to equal the token's hash. Return only the current recomputed IDs. No cursor, selection, amount, Draft or action authority is renewed from token data.
- Required failing-then-passing local proof: unchanged membership at an older token revision succeeds with current request context; changed membership, future token, missing/malformed revision, candidate/session/version/scope/kind mismatch and stale request context reject. Ordinary paging cursors remain strict. Public/controller integration and representative performance remain separately gated.
- Result: the intended old-token/current-membership call first failed with STALE_CURSOR. The narrowly scoped reader change then passes all three source/runtime tests with no skips: 13 token-binding negatives, stale request context, equal-count/different-ID selection, exact 35-ID return and unchanged strict generic cursor checks. A test source guard was corrected to inspect the function body rather than matching its legitimate GRANT EXECUTE declaration.
- Wider regression: 131 backend boundary/selection/projection/Timesheet/Draft checks and 295 frontend component/frozen-source checks pass, no skips. The existing frontend and Worker Draft owners remain unchanged. D1 technical self-review: current request authority and current selected membership remain mandatory; no action or financial authority is imported from the older token. EVIDENCE-0034 / DEV-0005. Full application wiring and performance still require proof.

## STEP-020 — contained screen coordination and atomic Draft-review handoff (COMPONENT PASS)

- Requirements: BP-001/007/014/015/016/023/024/026/034/035/037/038/040/042/050/053/077/102/121/130/131/132. Existing worktree/instruction/source baselines and Create Draft contract in document 23 remain controlling.
- Planned files: the previously absent js/banking-pay-modal-v2.js controller and focused controller tests. Compose the already-tested table/child/issue/settlement contracts; do not create another selection owner, renderer economics or legacy action implementation.
- Bounded purpose: construct exact read contexts, preserve independent main/child/list paging, prevent stale navigation and Timesheet publication, send every mutation through the existing shared settlement queue, and provide the immutable Draft review in the same prepared synchronous adoption as all views.
- Initial controller transport/action/view adapters remain explicit seams until their exact main.js integration is proved. No module load, partial v2 activation, unsupported extra reconciliation request or changed legacy Draft owner is permitted.
- Required proof: first summary one read; sort/page over server results; selected-only shortcut from current state; child close retains main state; issue-only search never changes payment filters; one mutation owner; lost response read-back without retry; all returned pages and Draft review share one revision; close/superseded responses cannot publish; malformed responses fail visibly. Existing full application tests remain red until actual adapters and all server envelopes exist.
- Implemented the contained controller, explicit existing-auth transport adapter and focused tests. The shared settlement helper gained only local view dismissal: closing a child cannot cancel a submitted mutation, clear an uncertain-outcome guard or let a late result reopen it. JSON action requests are captured when queued, not reread from subsequently edited form state.
- Failing-first tests exposed the absent controller/transport, an incomplete UUID validation expression, busy-child dismissal and mutable queued payload. Each is fixed and its test passes. Controller19, transport18 and existing settlement37: 74 checks pass with no skips. The final broader frontend component/controller/transport/frozen-source run passes 332 with no skips.
- Eight further in-memory tests send the actual frontend request builder through the actual Worker router and response validator, with explicit synthetic RPC replies. All pass with no skips. This proves route names, argument separation, selected-only scope, one-RPC dispatch and exact candidate POST fields; it is not browser, installed-RPC or deployed evidence.
- No main.js integration, feature activation or original Draft-owner edit occurred. The actual legacy action/DOM adapters, full public server envelopes, open-child mutation response/payload budget and installed/deployed acceptance remain gates. EVIDENCE-0035.

## STEP-021 — filtered all-pages header selection (COMPONENT PASS)

- Requirements: BP-007/025/026/027/033/037/038/040/042/050/053/077/130/131/132; main Include header contract and document23 Draft invariants. Backend baseline is now exact 0f5b255d, preserving the complete AV-961 release. No public activation or original legacy request-body change is authorised by this step.
- Integration risk: the legacy global branch selects across the session without the active pay-channel filter. The new header must select/clear the complete filtered eligible Ready set across candidate pages. Repeated candidate requests would introduce multiple revisions and partial completion and are prohibited.
- Planned change: extend the private v2 branch of the existing selection owner to handle an explicit full-filtered-scope intent through the same implementation as candidate intent. Lock the session/scopes/rows, apply the set-wise selection once, invoke the existing recovery revalidator once per affected candidate (as the old global owner already does), then publish one final full-session selection identity set, revision and audit. No financial expression or other legacy branch changes.
- Required proof before dependent work: more than 100 candidates and payments; all channels; Candidate/Client filters; hidden/nonselectable/dirty and out-of-scope rows unchanged; promotion/demotion and explicit overrides; exact full-session Draft selection; stale/conflicting/replayed/no-op requests; a failure after earlier candidates were processed rolls back the whole operation; no browser fan-out. Public response bounds and actual header/settlement integration remain separate gates.
- The new source/runtime checks first failed because no global dispatch existed. The existing private v2 selection implementation now accepts a closed modal_global_intent_v2 request as well as candidate intent. Every pre-existing public selection request branch remains byte-identical. There is no repeated candidate-selection call or new financial calculation.
- The full-scope fixture covers 106 candidates / 210 PAYE payments, recovery promotion and demotion, unchanged Umbrella rows, all-page explicit overrides, idempotent retry, true no-op, seven malformed/stale/conflict negatives and rollback after an earlier candidate has already been processed. Full-session Draft bookkeeping remains 109 selected outside-PAYE payments after clear and 320 after the PAYE select/promotion.
- Twelve further SELECT/CLEAR scenarios cover ALL/PAYE/UMBRELLA crossed with no filter, Candidate, Client and combined filters. Hidden snoozes, Cases, ineligible payments and out-of-filter ordinary payments retain their original rows. Dirty/Updating candidates are not targeted or revalidated. An exact empty scope is a no-op.
- The original candidate runtime and negative fixtures still pass, including subsequent original individual/global operations. A 34-test combined selection/recovery/projection/Timesheet/Draft run passes with no skips. Both existing Create Draft owners remain unchanged. EVIDENCE-0037.
- One wider run exposed a fixture timeout: 26 mutations were bundled inside one 30-second DO statement. The matrix now calls each scenario as a separate statement under the same 30-second limit, with rollback restoring its starting state. No application timeout, guard or assertion was relaxed. Cumulative fixture timings are not per-click latency or performance acceptance.
- Still open: actual public header response/transport/controller integration, compact movement/receipt bounds, representative per-click timing, concurrency and deployed acceptance. This component pass is not a whole BP-row pass or v2 activation.

## STEP-022 — reject incomplete or falsely empty Ready pages (COMPONENT PASS)

- Requirements: BP-016/027/033/077 and truthful loading/empty/error states. No product, wording, amount, selection or Draft owner change.
- Boundary review found that a Worker fixture claimed two Ready payments while returning one on its first and final page. Another frontend-to-Worker fixture claimed one payment with an empty child. The child presenter already rejects falsely empty data; the Worker and database must reject it consistently before adoption.
- Planned files: Worker Ready-page response validator and fixtures, public Ready reader's terminal-cursor guard, and rollback first-use proof. First/final pages must be complete; positive totals cannot produce an empty Ready page. A valid current row used as a cursor after the final row must reject as stale navigation, not claim the candidate has no payments.
- Required proof: malformed empty/short/over-count responses rejected; ordinary first/continuation/last pages retained; departed candidate remains a truthful empty response; no changed financial or mutation authority. Then rerun Worker, actual frontend request-boundary, original Ready/Timesheet and Candidate presenter tests.
- Four malformed Worker responses first returned success and the real local terminal-cursor call returned a falsely empty page. Both boundaries now reject them. A real last continuation page still returns its smaller page with the complete candidate header; a departed candidate still returns the existing truthful empty response.
- Corrected two synthetic response fixtures to contain the rows they claimed. The complete-header test now requests a real one-row page with continuation rather than disguising missing data as a final page. A separate test preserves legitimate last-page behaviour. No production row or original owner changed.
- Combined Worker108, request-builder boundary8 and Timesheet/Ready3: 119 checks pass with no skips. Candidate/controller/settlement67 also pass. A first rerun inherited a command-scoped NOTICE suppression used for the local installation and hid a test's success marker; rerunning with its normal environment passes, without changing that assertion. EVIDENCE-0038.

## STEP-023 — open-child mutation response budgets (APPROVED; IMPLEMENTATION IN PROGRESS)

- Requirements: BP-027/033/038/040/077/130/131/132, the one-mutation request budget and document10 payload limits. No budget exception or product change is approved by this entry.
- The candidate-wide checkbox also exists inside Candidate Banking. Its accepted response must update the still-open complete breakdown, not only the main-row amount. The current 32KB candidate-selection investigation threshold must be tested against that real response shape; an extra automatic child GET would violate the normal request budget.
- Planned read-only measurement of synthetic local rollback fixtures: collect the exact current Ready page before/after the actual candidate selection owner; measure the complete page, ordinary structural patch and transferred compressed sizes without printing payment payloads. Determine whether the existing limit can be met by a safe bounded representation. Do not mark a cosmetic revision stamp as updated financial data or relax a gate silently.
- Measured the existing 105-payment fixture, with 100 rows open. After the actual local candidate-selection owner: complete child175286 bytes including the internal selection result; ordinary structural patch36541 bytes including that result. Both exceed the32KB candidate-selection threshold. Brotli-compressed experiments were5470 and1759 bytes respectively; these are local compression measurements, NOT observed Worker/network or latency results.
- A more specialised compact patch protocol remains a technical alternative, but is not implemented or proved. Requested explicit user approval for a narrow D2 alternative: retain32KB for the candidate-selection authority/main update; when a breakdown is already open include its bounded complete page within the existing Ready-page limit, in the same response. No extra automatic request and no relaxation of latency, memory, one-revision or zero-loss requirements. No exception is approved yet.
- Dependent open-child mutation-envelope implementation is paused pending DEV-0007. Unaffected Action/Blocked/read integration and tests may continue. EVIDENCE-0039 contains reproducible numbers and exact local source hashes; no hosted mutation occurred.

- Subsequent explicit user approval: `YES AND CONTINUE` on 28 August 2026. DEV-0007 is now approved and the earlier pause is lifted. Documents01/04/10/17 and the deviation register now retain the compact base16/32KB plus optional already-open Ready256/512KB, combined272/544KB, with every request/latency/memory/financial/Draft fence unchanged. This approval is not an implementation or performance pass.

## STEP-024 — enforce approved selection/open-breakdown boundary (COMPONENT PASS)

- Authority: full implementation and explicit DEV-0007 approval. Requirements BP-027/033/038/040/077/130/131/132; candidate selection/Ready request and response contracts; no new visible wording or Draft input.
- Baseline: frontend b66b2a61d3d266ef4e9a3d410061274ca74a7a8e; backend0f5b255d1e09f425bf990cabf8f6e1f1e2473343. Existing tracked broker integration and main.js remain unchanged. Prepatch frontend controller SHA8e25450c2562116c7f57a82a107dcb484fb0638133bcf8da098e552ffed712e8; Worker module SHA1f6383d892d951785755f46b33dafb0b34d620d11e11f39d1e1aed53cd7f378c. Fresh permanent Miget parity and both TEST infrastructure inspections authenticate and report healthy services; no hosted write.
- Planned files: contained Worker and frontend transport, focused boundary tests. Add optional strictly shaped open_ready navigation metadata to the candidate request; require a complete same-candidate/same-revision child only when requested. Keep one RPC/HTTP request and unchanged original financial/Draft owners. Validate exact changed/no-op revision and accepted byte limits before publication; a rejected POST reply remains uncertain and requires read-back.
- Failing-first cases: request currently rejects open_ready; unsolicited child and oversized base currently pass unchecked. Prove complete/first/final child, wrong candidate/revision/header, extra/missing child, Unicode byte sizes, at-limit and over-limit responses, no second RPC and no blind retry. Full SQL/public-envelope and actual application integration remain separate gates.
- Retrieved current Cloudflare best-practice documentation and types5.20260828.1 into a task-only temporary directory; no project dependency/configuration change. Skill guidance does not authorise unrelated Worker/auth/database changes.

- Failing-first evidence:21 of30 Worker selection-boundary checks failed for the intended missing input/response/size/revision safeguards; the frontend positive open-child transport check also failed. All now pass. Combined Worker/selection/request-builder147 and frontend transport/controller/settlement/Draft113 checks pass with no skips. The new synthetic100-row payload crosses the actual frontend builder and Worker in one request. No public SQL mutation envelope or application DOM wiring is claimed by this result.
- Self-review PASS for this component: closed input shape; exact changed/no-op revision; no financial arithmetic; complete same-revision candidate/child agreement;32KB base and512KB child enforcement uses UTF8 bytes; no duplicate request or unsafe no-write claim. EVIDENCE-0040. DEV-0007 remains pending full integration/performance acceptance, not pending user approval.

## STEP-025 — retain Ready page position across selection revisions (COMPONENT PASS)

- Requirements BP-016/027/033/038/040/077/130/131/132; approved DEV-0007. Add only server-owned Ready page-position metadata to the existing Ready reader. Preserve every current line payload, candidate amount/count/classifier and ordinary strict revision-bound cursor.
- Current head/instruction authority remains STEP-024. Planned file: existing structure-v2 SQL plus focused rollback fixture/source tests. Prepatch SQL SHA16d7f1d619bca38c0acd89ff7a6780f05f1940d51eb06a659f6be721f6ded685. No original Workbench/Draft owner changes.
- A separately typed READY_PAGE_ANCHOR may renew only read position across progress revisions with exact current request authority and same session/version/candidate/filter/page-size. It conveys no financial fact. Keep the anchored row's current aligned page; if it leaves Ready backfill from its former position, falling back to the last remaining page or truthful empty result. Server Previous/Next metadata avoids an old-cursor history stack.
- D1 DEV-0008 requires failing-first and executable equivalence before dependent mutation assembly. Test old ordinary cursor rejection, current anchor renewal, all binding negatives, row movement/removal, final-page disappearance, payload parity and no writes from reads. No new automatic HTTP request or performance waiver.

- Both source and actual rollback first-use tests failed first because Ready page-position metadata was absent. Added only the separately typed position anchor and server Previous/page metadata; original row payload projection and normal exact-revision cursor are preserved.
- Real selection proof:105 Ready rows across three40-row pages; selection changes all105 and the renewed second page retains its exact identities with current amount/selection. Twelve malformed/cross-scope/future binding cases reject; old ordinary cursors reject; removed anchor backfills; vanished last page uses the last current page; final departure returns truthful empty. Reads leave fixture rows unchanged.
- Combined local Ready/Timesheets/Draft/global/candidate/recovery32 checks pass with no skips. The original complete Ready payload equals the legacy reader. Self-review PASS for D1 DEV-0008 and this component only; representative timing and full public mutation/application integration remain open. EVIDENCE-0041.

## STEP-026 — retain current bank issues with no physical payment row (COMPONENT PASS)

- Requirements BP-090/095/097/099/100/121 and the absolute indefinite-snooze/filter boundary. Existing `collectPayeeReadinessBlockedLines` enumerates stored current payees independently of physical Ready/Blocked rows. The new private collector currently joins every stored payee to an eligible physical candidate, which can omit that existing presentation before rows are available.
- Scope: change only the private bank-source collector and its rollback/source/oracle tests. Permit an independently current, visible stored candidate/payee when no physical row exists at the accepted version. A candidate with physical rows that are all hidden, excluded, out of scope or otherwise ineligible must not gain this fallback. A hidden candidate fragment/payee remains excluded. Preserve exact route, bank hash, current source/version and candidate/client/channel filters.
- Failing-first proof: unchanged legacy collector retains a bank-only source; the new database collector must do the same without fabricating a payment identity or amount. Test no-row, hidden fragment, hidden physical rows, wrong client/channel/candidate, stale source/version, and original payload equality. No bank action, financial calculation, Draft contract or public activation changes.
- A source-only task must not fabricate an affected-payment count of one. Its eventual list/detail contract must distinguish current known payment membership from not-yet-available membership; that presentation integration is a separate gate.
- Failing-first: the real local collector returned104 instead of105 affected candidates when one physical row was removed, while the unchanged legacy collector retained the stored bank issue. The corrected collector and wider bank/visibility suite pass11 tests with no skips. No synthetic payment ID or amount is created; hidden physical rows and hidden candidate fragments stay out.
- A fixture exposed an existing filter detail: a stored bank source with no published client identity is retained by the legacy client filter; a known different client is excluded. The new collector keeps that exact rule, tested against the unchanged JavaScript oracle. The test was corrected instead of narrowing the existing filter's functionality. EVIDENCE-0042; self-review PASS for this private component only.

## STEP-027 — finance task grouping from existing action guards (COMPONENT PASS)

- Requirements BP-080–089/099/100/101/102 and existing resolution/component controls. Use the already-characterized case and component guards to identify mandatory human tasks before pagination. Optional Clear/Exclude controls alone must never create an Action Required task; they remain available in full detail through the existing renderer/handlers.
- Planned changes: expose the already-selected canonical component array in the private case metadata; add a private finance-task projection and complete set-wise current-row collector; add failing-first oracle/grouping/runtime fixtures. No amount, rate, recovery, case mutation, Draft or original handler is changed.
- One case-level task contains all of its current row/component evidence. Component-only tasks retain exact existing component identity and source basis. Keys are presentation/task keys, not financial economic keys. Conflicting case-owner aliases fail visibly instead of arbitrarily merging unrelated cases. Hidden/current-version/current-source/filter checks precede grouping and pagination.
- Required proof: all existing case families; component-only manual/suggested actions; optional-only/fixed/no-action states; repeated case presentation; different case/component/basis identities; more than100 rows; hidden/filter/current-source exclusion; complete unchanged payloads and no writes. Public task/bank/source assembly and real action wiring remain separate gates.
- Failing-first: the projection was absent; the first complete grouping then exposed two tasks for one case when a component-only row coexisted with a case-level action. The set-wise collector now uses the primary case task and retains every related row.14 focused/wider checks pass with no skips, including72 action combinations compared with the unchanged renderer and105 case presentation rows in one task.
- The complete fixture also contains a related passive deduction and a Ready payment. Both remain detail context only, with explicit `context_only=true`; neither acquires Action Required ownership, loses selection eligibility, or changes its amount. Conflicting canonical case aliases reject typed. Current source/version, filters, hidden rows, component-basis keys and full original row-payload equality pass.
- Two test-harness defects were corrected: a text argument was accidentally sent as JSON, and cross-VM objects needed JSON normalization for strict equality. Neither changed application behaviour. Self-review PASS for this private component; EVIDENCE-0043. Complete bank/source task classification, list/detail API, action wiring and performance remain open.

## STEP-028 — exact current bank-target and check-version facts (COMPONENT PASS)

- Requirements BP-090–097/099/100 and existing bank-review/setup controls. Current source and handlers prove that bank actions bind the exact candidate/umbrella owner and bank-details hash; the approved one-off payout route is also an existing target, not interchangeable with the candidate's normal bank account.
- Planned files: a private set-wise read-only projection of current target, name-check version, exact-hash override and mapping presence; rollback/negative fixtures. Reuse the current Candidate/Umbrella records and `v_finance_cases_register` one-off edit gate. Do not copy bank fields, override reasons, provider IDs, credentials or raw result payloads into these facts. Do not call a provider or change any record.
- A known target, a successful name check, a current override and a payee mapping are separate facts. Provider/environment/hash and candidate-owner links must all be exact. A result from another hash/provider/environment must not approve this task. This stage grants no new action and does not replace the existing authenticated handlers.
- Failing-first source/runtime absence was reproduced after fixing fixture setup to insert initially Umbrella candidates instead of bypassing the existing pay-method-change operation guard. Synthetic bank fields use unusable zero account details and the normal hash trigger; no bank constraint or trigger is disabled.
- Actual PostgreSQL proof covers candidate and umbrella ownership;105 candidates sharing one umbrella without lost/duplicate targets; exact name-check versions; separate mapping/override facts; wrong hash/provider/environment/owner; inactive umbrella; and an actual one-off finance case whose current view permits editing before, but not after, its simulated paid state. All fixture state rolls back. The existing constraint itself rejects an attempted wrong-hash override, which is tested rather than weakened.
-14 combined bank-target/source/action/readiness tests pass with no skips. Self-review PASS for this private read component only; EVIDENCE-0044. No real payment/Draft/provider call, hosted write, public action grant or activation occurred. Current job ownership and complete task classification remain open.

## STEP-029 — distinguish current bank work from stale job labels (COMPONENT PASS)

- Requirements BP-090–098/099/100. A stored word such as Running is not enough to show Updating. Reuse the existing PAYEE_READINESS_ENSURE job type, exact session/candidate/source generation, original payee fingerprint, executor provider/environment and completed/successor precedence. Do not enqueue, retry, repair or run a job from a read.
- Planned files: private set-wise bank-job evidence plus rollback first-use/negative fixtures. Preserve the executor's25-payee bound; a payload that cannot fit that existing unit must not be described as progressing normally. Caller must combine this evidence with the exact current bank target and canonical source/task guards before publishing Updating or any action.
- Failing-first absence was reproduced. The current table uses SUCCEEDED for completion and DEAD for exhausted work; fixture and classifier now use those exact installed states instead of an assumed Ready/ERROR job label. No table constraint was changed. Failed and DEAD jobs are never reported as progressing.
- Real local proof covers exact session/candidate/version/source generation/payee fingerprint/hash/environment, wrong explicit payee candidate, active successor and completed-equivalent precedence, oversized26-payee units, and the executor's existing default environment. Reads do not change jobs or invoke the executor.31 combined bank/finance/source-progress/Create-Draft checks pass with no skips; original Draft contract remains unchanged. Self-review PASS for this private component; EVIDENCE-0045.
- Source integrity check passes202 migrations/429 repeatables; these are repository counts, not a hosted release. Main source LF-normalized hash remains eaa48859cd2b2d9fb4bc4ed33727ab724d4d05041695712e2cbaa18f89e76bae (raw CRLF/mixed-ending file hash e837da26a5c0ddf9d9d559ebc8c2968e035ab348a40e661688d71b1af9a8ebee). Tracked broker integration hash remains a3cd82db95b43a3dbb72cceb12158b5f658e44de154b00d88106389a2a155260.

## STEP-030 — current shared bank-problem classification (COMPONENT PASS)

- Requirements BP-090–100/102/121. Combine the proved scoped bank sources, exact bank target/check versions and current job evidence. Group one shared account problem before pagination, while retaining each affected candidate and original source payload. A current running account operation is Updating; failed/terminal/stale evidence alone never grants a retry or approval.
- Existing bank action guards remain controlling. Missing details and inactive umbrellas lead to the existing Candidate/Umbrella record editor, not a new bank-write authority. A stale owner/hash/result cannot receive the old Accept or Set up action. Related Ready context retains its original economic owner. A synthetic source never invents a payment ID or amount.
- Private classification/collection and failing-first tests only in this step. Complete affected-payment joining, source-only unknown-count presentation, list/detail APIs, source-progress issues and all actual action wiring remain required before activation.

- Failing-first absence and a real cross-filter failure were reproduced. A running operation on a shared umbrella account must remain Updating when the initiating candidate is outside the current display filter. The private job reader now matches that exact shared owner/hash only after proving the initiating candidate's current umbrella link, original source counter, payload, session and fingerprint. This does not return the filtered-out/hidden candidate or grant a new action.
- Seven combined local checks pass with no skips:24 explicit current/stale/failed/editor cases,105 shared candidates in one task, unchanged source payloads, source-only membership, changed result/task identity, filtered initiating candidate, indefinite-snooze and channel exclusions. The SQL-null test harness was corrected to serialize JSON null; no application safeguard was removed.
- Existing Accept/Check/Set-up guard names remain unchanged. Editor navigation requires explicitly supplied existing availability; private metadata is not permission to mutate a Candidate/Umbrella record. A stale bank target/result or oversized job offers no approval/setup action. No payment, selection, Draft, bank or provider write is performed. Self-review PASS for this private component; EVIDENCE-0046. Grouped public list/detail and complete affected-payment references remain separate gates.

## STEP-031 — bounded complete task detail and honest payment counts (BOUNDARY COMPONENT PASS)

- Requirements BP-090/095/098/099/100/102/121/130. Current bank-source proof demonstrates that a real task may exist before a physical payment is published. The Action presenter currently insists on a positive payment count, which would either hide that issue or invent a payment. Correct the contract to distinguish a complete known payment count (including zero) from incomplete/not-yet-published membership; use a neutral dash for the latter, with an explicit explanation in detail. Candidate count remains exact and positive.
- Every task detail remains complete through explicit bounded server pages; it is not permitted to send an unbounded shared-umbrella graph or silently truncate at100. Existing detail route already admits cursor/limit; document exact response/navigation and enforce it in both boundaries. This is D1 fulfillment of existing complete-detail and bounded-response requirements, not a removal or added main column.
- Planned failing-first frontend/Worker fixtures cover zero known payments, unknown count,105+ affected members, stale/sort/scope cursors, invalid count metadata, contradictory detail page counts and source-only detail without a fabricated payment identity. No product activation or claim of full list/database integration follows from boundary tests alone.

- Failing-first unknown/zero-count and incomplete-detail checks were reproduced. Added the exact complete/unknown count flag, dash with `Payment count not yet confirmed.` and100-member/256KB complete detail-page validation. Existing positive-count fixtures now declare completeness explicitly. Original payloads remain unchanged; only the new wrapper carries source/context metadata.
- Combined Worker/transport/selection suite192 checks passes with no skips; frontend count/detail/list/controller/transport suite121 checks passes. A subsequent actual controller test also passes: it adopts100 of105 members, rejects a malformed continuation, retains its previous accepted page and dispatches no mutation. Updated that suite now contains20 checks. Tests are synthetic boundary/component proof, not deployed/server-detail parity or browser performance.
- D1 DEV-0009 and EVIDENCE-0047 record these safeguards. No financial count is inferred from a loaded page, no source-only payment is fabricated, no main column is added, and no original bank/case/Draft handler is changed. Self-review PASS for boundaries only. Actual complete scoped membership, server cursors, original detail actions and browser layout remain open.

## STEP-032 — source-work ownership and complete issue catalogue (IN PROGRESS)

- Requirements BP-080–102/121/130. Complete the Action/Updating/Blocked read catalogue over the current source/case/bank owners, with one effective section and explicit context references. Current source work must be distinguished from a stale pending word; retain legitimate scope-seeding, source-build, line-work, materialisation, delta and clone stages without inventing a retry action.
- Inspect the existing continuation and progress owners before choosing current-job predicates. Test actual rollback-contained continuation payloads, missing/terminal/wrong-version owners, valid successors and all-hidden/filtered sources. This step must not execute a worker/provider, create a Draft, mutate hosted data or change any existing job/finance policy. Public summary/list/detail assembly follows only proved complete source coverage.

- Stage-job component: seven actual existing continuation families now pass local rollback tests, including49 contradictory context cases, valid successors, completed equivalents, absent optional sequences, missing clone sources and stale/failed private build phases. Six combined stage/source-owner checks pass. Fixture setup retained all real build/cursor/lifecycle constraints; no executor or provider was run.
- Failing-first source/Ready integration exposed `NONCURRENT_SOURCE_STILL_READY: FAILED`: the initial new Ready collector excluded only a short pending-status list. It did not reproduce the existing failed-candidate disabling rule. Replace that partial new predicate with the already-proved complete source-owner facts (`source_state=CURRENT`) before aggregating Ready or admitting candidate selection. This is a correction to the new implementation, not an alteration of the original financial/Draft owner. Source-only and previous-payment context remain visible separately; full regression proof is required.
- The source issue collector now retains110 complete source/context members for one candidate, one shared published Refresh task across affected candidates, and honest unknown payment counts during source rebuilding. Current stage work takes Updating ownership; a failed/stalled source gets Action Required only when the existing progress owner publishes Refresh/Retry and the existing adapter is available. Otherwise it remains a visible passive issue. No retry or work is invented by this read.
- Ready summary membership, Ready child reads and candidate-wide selection now share the complete current-source fence. Eight failed/pending source states cannot remain Ready or accept candidate selection. Current, hidden-indefinite, source-only, candidate/channel filters and no-write checks pass. Combined local stage/source/global-selection/Ready-anchor/Draft suite:32 passed,0 failed,0 skipped. An initial invocation lacked the frontend-root test opt-in and skipped3 frontend Draft cases; the complete rerun supplies it and exercises all3 successfully.
- EVIDENCE-0048 records this local component result. STEP-032 remains IN PROGRESS: unified bank/case/source/passive list assembly, exact affected membership, public envelopes and browser/application adoption are not yet complete. No source helper is a new financial, job-execution or Draft authority.
- Complete shared-bank detail now retains210 source/payment-context members for105 candidates. Exact row owner/hash routing identifies affected payments; other candidate payments remain labelled context, not silently assigned to that account. Unknown owners retain candidate-specific keys. Mixed current/stale bank results remain one task while the stale member cannot borrow an approval action.
- A compact issue-reference index now combines current bank, finance and source tasks with passive physical blockers before count/pagination. A related passive recovery remains Blocked; resolved/component-only CASES context stays with its primary case instead of being counted again as Blocked. Shared bank work contributes one Updating task. Twelve combined index/bank/finance/source checks pass,0 skips (EVIDENCE-0049). Public list/detail envelopes, exact count metadata for each family and full action rendering remain open.

## STEP-033 — public main summary over the certified owners (COMPONENT PASS)

- Requirements BP-015–042/130 and Create Draft supplement23. One service-only public reader composes the existing candidate page, complete issue-reference counts and original read-only Draft-readiness owner. First-page scope discovery and later strict cursor/revision/actor checks are retained. Rail defaults come from the existing settings owner, not caller input; no provider is contacted. The complete response is capped at128KB and cannot be silently truncated.
- Two source/runtime tests pass with no skips. The real local public RPC returns105 candidates over100/5 pages; all nine captured responses pass the actual frontend and Worker validators. The full headline is£2,120.00 while the first visible page sums to£2,070.00, proving that off-page selection is included. Six sort/direction combinations preserve the full global values. An actual candidate CLEAR changes the headline to£1,050.00 and retains that candidate as NONE/£0.00/no deduction. Existing Draft-gate equality, stale/auth rejection and unchanged payment rows after reads are verified.
- The first fixture run correctly rejected103 incompletely described new payment fixtures from Ready. The fixture was corrected to provide the existing canonical Ready presentation/attestation fields; no eligibility guard was weakened. EVIDENCE-0050 is local public-RPC/boundary proof only. The main application is still unmodified and v2 unavailable; actual route/DOM/browser/performance/deployment acceptance remains required.

## STEP-034 — complete public Action/Blocked detail readers (COMPONENT PASS)

- Requirements BP-090–102/105–123/130 and DEV-0009. Add one exact-key, service-only detail reader over the proved finance, bank, source and passive member authorities. It must preserve every original member payload and action guard, retain explicit context, page before serialization, and reject stale/cross-item/cross-scope cursors. No mutation, payment calculation or invented source payment ID is permitted.
- Characterize the missing public functions before implementation, then prove real105-plus member continuation, unchanged original payloads, exact current counts or explicit unknown counts, hidden/filtered exclusion, stale/auth failures and both actual boundary validators. Limit each explicit detail page to100 members/256KB. Application action/render wiring remains a separate gate.
- Selection response investigation remains open: an unbounded movement list cannot fit the agreed32KB base reply. Do not cap candidate membership, silently truncate movements, increase the budget or activate a partial workaround. Resolve and prove the bounded reconciliation mechanism before the public selection path is adopted.
- Actual local public detail proof retains106 finance members across100/6 and40/40/26 pages, including105 affected payments plus passive context. The shared bank proof retains210 members across three pages and distinguishes a confirmed105-payment count from an unknown count. Failed-source detail retains110 members without inventing payment membership. Five actual public responses pass both frontend and Worker validators. Original payload equality, all cursor bindings, wrong task/section, current revision and admin rejection are covered. EVIDENCE-0051; action rendering and hosted/browser acceptance remain open.

## STEP-035 — repeated-read performance regression diagnosis (LOCAL COMPONENT PASS)

- Requirements BP-016/038/130/131 and performance protocol10. A combined local run passed the new bank/detail checks but timed out in the existing nine-summary fixture. A separate summary rerun passed. Instrumented repeat calls then reproduced a warm-query regression: approximately0.99 seconds initially became3.85–3.94 seconds after cached-plan reuse; issue-index work grew from0.49–0.65 to3.24–3.33 seconds. JIT-off alone did not remove it.
- A rollback-only diagnostic with parameter-specific planning kept six complete summary replies identical and within0.82–0.96 seconds. This proves a local planning sensitivity, not hosted/browser performance acceptance. Next isolate the smallest new function scope; do not change database/session-wide configuration, raise the test timeout, weaken financial predicates or treat the rerun as explaining away the failure.
- The first scoped planning change preserves the exact certified predicate and restores the caller setting, but the wider43-test run still fails one summary sort. Inspection proves the existing PREVIEW_PROGRESS owner sets the next statement's budget to3 seconds; the fixture's earlier30-second comment was therefore inaccurate. The timeout remains unchanged. Investigate unnecessary source-detail expansion when the current source owner proves there are no noncurrent sources, then repeat the complete run.
- The SQL-language wrapper retained the warm regression despite its function setting. A rollback-only PL/pgSQL RETURN QUERY wrapper over the identical certified statement reduced repeated section checks from roughly2.93 million to75,444 in the instrumented comparison. The adopted change affects only this new private reader, retains STABLE/INVOKER/empty search path, and restores caller planning configuration. No legacy function body, eligibility predicate, finance equation, timeout or database-wide setting changed. A current-source early return avoids expanding three payment sections only when the same stable owner proves no noncurrent source exists.
- After the narrow change, all43 combined database/boundary tests pass with zero skips, including global selection, complete detail, Ready movement and the unchanged Create Draft contract. Source-state proof completes in4.73 seconds for its entire multi-state fixture; nine public summaries complete in9.14 seconds with the existing per-operation budget. The six sampled complete summaries were0.78–0.95 seconds. These are local synthetic measurements, not representative hosted/browser acceptance. DEV-0010 and EVIDENCE-0052; see the measurement JSON and document10.

## STEP-036 — reject incomplete issue-list pages (BOUNDARY PASS)

- Requirements BP-102/121/130 and zero-loss paging. The initial frontend fixture accepted one row with total101 and has_more=true under a100-row limit. This would conceal99 members on a partial response. Replace that fixture with a real100-row page and add failing-first truncation, page-position, Previous and first/last-page checks at both boundaries.
- Require server page metadata and exact page cardinality; pass the requested cursor/limit through the controller validator. This does not add a search, change existing filter/Draft scope, change user copy or permit a financial recalculation. Rejected navigation retains the previous accepted page.
- Failing-first truncation and page-position checks were reproduced at both boundaries.194 Worker/boundary/selection tests and155 frontend issue/controller/copy tests passed at this step. A truncated refresh retains the previous graph and does not enable a mutation or Draft from partial data. Public issue-list SQL and integrated browser proof remain open; EVIDENCE-0053.

## STEP-037 — physical payment identity across detail surfaces (BOUNDARY PASS)

- Requirements BP-041/042/130 and one-effective-section preservation. New issue detail IDs are task/member presentation keys, whereas Ready identities are physical preview-row IDs. Comparing only presentation keys can miss the same payment in both sections. Add failing-first checks using different presentation keys and the same exact preview-row reference.
- Compare existing physical references, never derive a new economic key. Explicit context-only detail members remain references and must not be mistaken for additional owners. Reject conflicting wrapper/payload identities before adoption. This changes neither section eligibility nor financial policy, and leaves existing legacy-identity checks in place.
- Ten failing-first cases reproduced the missed physical-identity collision or contradictory reference. The corrected settlement preserves explicit supporting context but rejects two effective owners before any commit. Frontend and Worker also reject a conflicting candidate ID inside a detail payload.118 focused frontend and49 Worker checks pass;70 of those frontend checks cover settlement/controller behaviour. EVIDENCE-0054. No main-app integration or production/hosted action occurred.

## STEP-038 — compact complete Action/Updating task summaries (COMPONENT PASS)

- Requirements BP-090–102/130. Build compact records over the existing issue index and complete finance/bank/source member owners. One underlying task remains one row regardless of affected-payment count. Compute truthful distinct-candidate and confirmed-payment counts before any list pagination. Unknown membership remains null, not zero or a guessed payment.
- Use only approved message titles or the current source-progress title. Keep original actions/payloads in the existing detail reader; the list grants no action and performs no payment calculation. Searchable candidate names/references stay server-side and never become main-table search or Draft filters.
- Failing-first checks must cover one106-member case versus105 distinct tasks; source-only/failed/Updating counts; shared-bank known/unknown membership; exact title parity; hidden/filter ownership; no per-task detail-RPC fan-out. Public paging, Updating view navigation, Blocked list and integrated UI remain separate gates.
- Seven combined local task-summary/bank/index checks pass with no skips. One106-member case retains105 affected payments, while105 separate cases remain105 tasks. Shared-bank counts distinguish complete105-payment membership from unknown membership; current Updating ownership remains separate. The test reads the actual trigger-generated candidate reference rather than assuming the inserted reference survives. All fixture writes roll back. EVIDENCE-0055; self-review PASS for this private component only.

## STEP-039 — reconcile the sealed Daily backend baseline (IN PROGRESS)

- Shared backend origin/test is freshly fetched at2a9b1589f09a42fb92252215c23c45b0cac86c1b. The only overlapping tracked path is broker/src/index.js; upstream changes its Daily materialisation function, while our known local patch changes the Banking import/router only.
- Preserve the complete owned Banking patch and every untracked file. Temporarily remove only those known owned hunks with apply_patch; require a clean tracked diff; fast-forward to the sealed upstream commit; then restore the same Banking hunks and compare the exact resulting patch. Do not reset/stash/discard unrelated files, push, deploy or run a hosted release.
- Re-read the added Daily AGENTS boundary and preserve all eleven repeatables, verifier, generated contract and synthetic alias. Re-run the unchanged Draft-owner and route tests after reconciliation. Installed local database replay still requires a later clean latest-head rebuild; a Git merge is not that proof.
- Fast-forward to2a9b1589 completed. The exact logical Banking-only import/router patch was restored; all untracked files were retained. Raw diff context/EOL changes were distinguished from code changes, and normalized added/removed code matched the saved owned patch.17 original Draft-contract checks subsequently passed with real local ALL/SOME/NONE fixtures; no Draft was created.
- New sealed Daily source2a6aef903b87568049ad5cd70bfbbc32b70fd84c has been fetched and its added AGENTS rule read. The Candidate/Client task also reports a pending/ongoing d947ce5c release. Neither is yet merged here. No Banking publication or hosted release is in flight. Reconciliation remains open until the newest sealed shared head is preserved and rechecked.

## STEP-040 — bounded Action Required and Updating pages (LOCAL COMPONENT PASS)

- Requirements BP-096–102/130 and existing separate Updating visibility. Add the service-only Action page over STEP-038 records, sorting/searching the complete task set before keyset pagination. Each view stays at100 rows and the combined response remains within256KB.
- Technical clarification: the same read endpoint accepts a strictly whitelisted view of ACTION_REQUIRED (default) or UPDATING. The ordinary Action response includes a separate bounded first Updating page and exact continuation metadata. Opening/paging Updating never changes selection or Draft filters; current detail remains available through the existing exact task-key route. Updating has no new search/sort controls and cannot borrow human-action ownership.
- Bind cursor to view as well as session/version/progress/scope/search/sort/limit. Never silently truncate Updating when more than100 tasks exist. First page, Previous, next page, missing boundary, unknown payment count sorting and literal search characters require executable proof. This adds no main-table control, main search, financial column, mutation owner or extra background request. Frontend navigation/both-boundary integration remains required before activation.
- Actual local RPC proof passes105 Action tasks over100/5 and40/40/25 pages; exact Previous round trip; literal percent/underscore search; empty results; changed view/search/revision/auth; malformed/missing/unaligned cursor boundaries; and unchanged payment rows. A second fixture adds varied titles, one/two/three-payment tasks and a two-candidate source task with unknown payment count: all six complete-result sorts retain106 unique tasks, with null payment counts last in both directions.
-105 actual queued source-work owners are created only inside the rollback fixture, without an executor. They remain105 separate Updating tasks with100 inline summaries and explicit continuation to the remaining5.17 actual public replies pass both Worker and frontend validators, including response size and current summary counts. SQL validation rejects null/unknown required task fields rather than allowing bool_and to ignore them.
- Worker/controller boundaries whitelist the view separately from Draft options. Updating has fixed title order and no search; incomplete/duplicate/cross-state Updating arrays reject. Explicit navigation uses one list request, preserves the previous Action query for Back, and opens the exact current task detail. No automatic background request or mutation is introduced.
-174 Worker/issue boundary tests and all364 frontend Banking modal unit tests pass, with no skips. The combined actual Action/Draft run passes19 checks with no skips. Browser component checks pass for both lists, Updating/back/hidden search and sort, exact detail keys, single-height44px task rows, full-text access, narrow overflow, rejected partial updates, focus and20 teardown/reopen cycles without duplicate handlers.
- These are local component results only. The browser used synthetic labelled fixtures and the real saved presenter, not the deployed Banking modal. No final-product screenshot, real action execution, hosted performance or full-product parity is claimed. DEV-0011 is locally equivalent; final application integration/release acceptance remains mandatory. EVIDENCE-0056.

## STEP-041 — passive Blocked display facts (PRIVATE COMPONENT PASS)

- Requirements BP-110–121/123/130; MSG-030/031/032/036/038/090–096; existing Blocked amount, Snooze and Timesheet controls. Add only private read projections and their failing-first characterization before assembling the public Blocked list. Preserve the complete original row in the existing detail reader.
- The unchanged renderer explicitly displays an overpayment's outstanding total when NO_PAY_HEADROOM prevents deduction. The general Ready display scalar is zero in that situation and cannot substitute for the Blocked display. Compare the new server projection with the unchanged outstanding/scheduled/section display helpers, including component and nested-alias cases. No recovery allocation or financial owner may change.
- Dated Snooze and Do Not Pay wording remains as currently displayed; approved replacements apply only to their exact known triggers. Unknown issues keep original diagnostic evidence but must not leak raw technical codes or acquire an invented action. Source-only failures carry no fabricated payment identity or amount. The final aggregate count wording for mixed source-only/payment blockers requires explicit reconciliation with BP-116/117 and the existing payment-count label before activation.
- Planned files: one private Blocked presentation repeatable, a set-wise compact Blocked member projection/public page after private proof, focused runtime/oracle/negative tests, and strict Worker/FE boundary checks. Current worktree HEADs remain frontend b66b2a61 and backend2a9b1589; owned dirty work stays intact. The shared Daily publication hold remains active. No hosted write, Draft, provider call, push or deployment is part of this local step.
- Failing-first absence was reproduced. Ten actual PostgreSQL display cases match the unchanged Blocked renderer's section, scheduled and outstanding branches, including nested aliases and component totals. Published loan nominal due is retained separately: it is not silently substituted into the existing displayed amount. Seven known/unknown/date/source/bank explanations, indefinite exclusion, corrupt amount and wrong-state negatives pass.26 combined presentation/original-amount/visibility tests pass with no skips. Two harness-only corrections normalized the old string scalar/negative zero and serialized SQL null explicitly.
- The helper exposes only approved/current wording and private diagnostic codes; it does not grant actions, change selection or mutate a financial owner. Self-review PASS for this private component. Complete member/list/boundary proof, full wording/action integration, count-label reconciliation and representative performance remain open.

## STEP-042 — complete passive Blocked list and boundary (LOCAL COMPONENT PASS)

- Requirements BP-110–121/123/130. Compose the existing issue index with exact source, bank and physical-row evidence in one set-wise read. Preserve original detail ownership, and expose no invented payment ID/amount for source-only failures. Candidate identity comes from the current canonical record.
- Add complete Candidate/Reason/Amount sorting and literal list-only search before100-row keyset pagination. Bind cursor to session/version/progress/scope/search/sort/limit; null amounts sort last both ways. Retain exact Previous, no duplicate/lost rows, hidden/Action/Updating exclusions and no partial response. The browser must reject malformed Blocked identities/reasons/amounts at the Worker and frontend boundaries.
- Planned proof: more than100 passive items, six full-result sorts,100/remaining and40-row Previous navigation, unchanged payload detail, source-only unknown amount, dated/indefinite snoozes, filters, stale/auth/cross-query cursor failures, complete summary count agreement and no writes. No new action, main column/search, Draft input, finance calculation or activation is authorised by this component.
- Actual local proof covers109 passive payment rows; six full-result sorts;100/9 and40/40/29 pages; exact Previous; literal search; dated and hidden indefinite rows; unchanged detail payload; stale/auth/changed-search/sort/missing-boundary failures; and unchanged payment rows after reads. Three stored account problems extend the list to112 entries with null amounts last in both directions, exact source-only detail and no invented payment. PAYE/UMBRELLA filters yield105/7; restoring the current reviewable name-check result removes the shared bank issue from Blocked and retains it under its human-action owner.
- All22 captured public Blocked replies pass both actual validators.14 failing-first Worker omissions and two frontend source-reference contradictions were corrected without relaxing financial checks. The combined Blocked/Worker/transport/Draft run passes221 checks with no skips; the subsequent expanded Blocked runtime also passes. A previously skipped cross-project Action fixture was updated to include its required Updating/view envelope, not accommodated by weakening validation. All366 frontend modal unit checks pass.
- Shared backend fast-forward now preserves f42436f3277a9d5011bc4056146193dfd78b0eb6, including d947ce5 and2a6aef90. The exact logical Banking router patch was saved/restored and compared equal, with all untracked work retained.31 post-merge Draft/Daily/Client checks pass. Source integrity is202 migrations/450 repeatables, not an installed hosted ledger. The Daily task retains the publication hold for a separate legacy-record read investigation.
- Self-review PASS for this local component. Full application action wiring, accepted wording/amount presentation, representative performance and release remain mandatory. User was asked whether a mixed source/payment Blocked count may say items rather than payments; no copy change has been made pending their answer.

## STEP-043 — main-page navigation after a selection revision (LOCAL COMPONENT PASS)

- Requirements BP-016/025–027/038/130/131. Existing ordinary page cursors correctly expire after selection changes. Candidate-sort single-response settlement must not leave Next/Previous holding those expired cursors, nor make the browser decode or rewrite opaque tokens.
- Extend only the already typed read-position anchor with a server-issued NEXT/PREVIOUS direction. Current request authority remains mandatory; ordinary cursors stay exact-revision. The server determines the current anchor page before advancing. Invalid direction, cross-scope/version/sort, future anchors and navigation beyond either end fail visibly. No selection, amount, filter or Draft semantics change.
- Planned proof first: actual105-candidate private/public paging across revision changes, same-revision equivalence, exact back/next, moved/departed boundary, strict negatives and bounded envelope. Existing mutation-response assembly must still prove unchanged other candidates before retaining main rows; this navigation work alone does not authorise retagging stale data. DEV-0013 is D1 pending executable equivalence.
- Directed anchors pass the initial105-candidate/current-revision and six-sort40-row equivalence checks. Ordinary cursors reject after revision changes, invalid directions and cross-bindings reject, and a departed boundary backfills without lost candidates. The strict frontend/Worker fields and controller use are covered by failing-first tests;372 frontend unit checks now pass. Older synthetic detail fixtures were updated to the complete response contract rather than making fields optional.
- Repeated sorted public reads twice hit the unchanged3-second operation timeout. The earlier generic-plan explanation was a hypothesis, not a proved cause: rollback-only comparisons subsequently pass with both explicit auto/custom planning AND the completely unchanged function configuration. Six sorted reads in the unchanged sample take0.85–1.00 seconds. No timeout, function or global planning setting, legacy SQL statement or economic owner is changed on this evidence. Repeat the full suite and retain this intermittent timing observation for representative performance testing rather than claiming a speculative fix.
- The complete standard regression subsequently passes151 checks with no skips, including eleven real public summary replies, current Previous/Next after an actual candidate CLEAR, the unchanged Draft contract and the newly merged Daily regression.372 frontend unit checks pass. Self-review PASS for navigation equivalence only; main-application mutation retention, final browser navigation and representative timing remain gates. EVIDENCE-0059.
- Backend is now fast-forwarded to sealed e2046c2d1409d6fd6d139d8ab66c8b3cb009f13e. The new Daily AGENTS rule was read, incoming paths did not overlap the owned Banking router, and its file hash was identical before/after. Both new Daily repeatables, current contract and NEW/UPGRADE verifiers remain intact. The shared hold is released; no Banking publication or hosted release has started.

## STEP-044 — bounded complete-selection settlement evidence (IN PROGRESS)

- Requirements BP-025–028/033/038/040–042/077/130–132; approved DEV-0007 response budgets and unchanged Draft contract. The private selection owner already processes the complete candidate scope and retains every physical movement in its current receipt. Sending that entire array over the network can exceed32KB for a large candidate, even without an open breakdown. Neither truncation disguised as complete nor an arbitrary payment-count cap is permitted.
- Proposed D1 mechanism: retain the complete movement evidence in the existing server receipt; return the full array when compact, otherwise an explicit incomplete-array flag, exact full count/digest and mandatory invalidation of ALL prior detail/section caches. The already-open Ready page remains complete in the same accepted response. Unopened lists are subsequently read in full through their normal user-initiated paging; no payment is removed from its server owner and no extra background read is added. Empty inline data must never imply no movement when the completeness flag is false.
- Before any main rows can be retained, separately prove that every other candidate's canonical display/count/Timesheet facts are unchanged. Tokens and revision labels are navigation metadata, not such proof. A missing or failed proof must prevent local row retention. Complete cache replacement, stale alias rejection, superseded/uncertain replies, large scopes, replay and no-op need executable tests before this mechanism is approved for public/controller integration.
- Planned changes are limited to new private response/facts helpers, explicit frontend/Worker response validation and their rollback/synthetic boundary tests first. Do not change financial equations, Draft arguments, source eligibility, scope filters, the original selection/recovery algorithms or a public activation flag. Current heads: frontend b66b2a61 and backend e2046c2d; owned dirty files remain intact. No hosted write/push/deployment is part of this local step.
- The private formatter passes empty/two/5000-movement and one oversized multibyte-row cases without altering original receipt evidence. It uses at most8KB for a complete inline array, otherwise explicitly returns incomplete with the exact count/digest and mandatory whole-detail invalidation. Malformed/missing/duplicate evidence fails. Real105-payment candidate selection/recovery/no-op/replay/rollback proof also passes through both actual response validators; the complete server receipt remains unchanged.
-177 Worker/transport/response checks,25 actual movement/selection/Draft checks and393 frontend checks pass without skips. The frontend staging helper discards ALL prior Ready/Action/Blocked detail caches, requires a complete current replacement for an already-open Ready view and refuses to silently close an Action/Blocked workflow. It is not yet wired into the main application. Other-candidate preimage proof, public response assembly, actual alias integration and full performance remain open; DEV-0014 is not a completed whole-system equivalence claim.
- The Daily task renewed the shared publication hold for a narrow factual-hours/manager-decision read projection. No Banking push or hosted release is in flight. The separate Candidate-change task reports a rollback-proved preview sequence invalidation (5 to6) in candidate_pay_method_change_refresh_scope_v1, while its HTTP timeout remains a separate investigation. This helper is not modified or owned by this Banking task.

## STEP-045 — current candidate-view preimage and retention proof (PRIVATE COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033/034/035/038/130–132. A current progress label alone is not proof that an old main row can be retained. Compare the complete canonical candidate facts before/after mutation, excluding only newly issued navigation metadata. This includes selected Timesheet membership beyond25 inline IDs, not just the visible count/amount.
- Planned private read helper returns bounded digests of the full candidate set and all candidates except the target, plus the target's canonical row. Hash the existing facts exactly; do not derive any new amount/eligibility or change a financial owner. Bind the digest to the current session/version/filter/channel scope. Subsequent public integration must pass the previously displayed whole-view digest and reject an unmatched preimage before submission, then permit retention only when other-candidate facts and membership are proved unchanged.
- Tests first: actual candidate selection changes the whole-view digest but not other candidates; another candidate's name, selection or exact selected-Timesheet membership invalidates retention even if the summary count stays the same; filters/scope bind the proof; no read mutation; no false proof from a changed progress label alone. Initial private-helper proof does not authorise frontend relabelling or loosen existing child validation. DEV-0015 remains pending full public and controller equivalence.
- Current backend e2046c2d and frontend b66b2a61; dirty work is preserved. Planned files: a new private candidate-state repeatable and focused local rollback/source tests before the summary/selection contracts or frontend comparisons change. Shared publication hold remains active; no hosted write or lifecycle action.

- Three local checks pass without skips. The negative test first reproduced acceptance of a negative session revision; the private helper now rejects it. Complete105-candidate scope, an off-page candidate, actual candidate CLEAR, same-count replacement among35 selected Timesheets, absent/empty targets, invalid context and no read writes are covered. Self-review PASS for this private digest projection only. Public preimage rejection, row-retention adoption and representative timing are not yet proved; DEV-0015 remains open.
- Sealed backend fedcb84c9142e391c4f5c1d1cea56a9ed8f62c64 is now fast-forwarded locally. The new Daily null-safe assertion instruction was read. The owned Banking router SHA256 remained FBA239EFD48F205D0FFBAB9A4671025E90D88A2AD834787855D884A5ED6D32D4 before/after; incoming Daily files and contract are preserved. No Banking publication or hosted release is in flight.

## STEP-046 — publish current read-content evidence without new arithmetic (LOCAL READ COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033/034/035/038/130–132, DEV-0015. Add the exact whole-view digest to the existing candidate-page projection using its already-materialized complete facts, and an exact facts digest to each canonical candidate row BEFORE selected Timesheet IDs are compacted. No second complete read, browser hash calculation, new financial scalar or selection/Draft change.
- Failing-first proof must show private state and public page digests agree across pages/sorts, Ready and summary rows share the same exact fact digest, and renewed navigation metadata does not claim a changed payment. Empty/missing evidence must fail response validation. Updating comparison/adoption is a separate dependent step: a digest is not permission to relabel stale financial rows.
- Planned source is limited to the existing unpublished structure reader, boundary validators/fixtures and focused tests. Frontend remains b66b2a61; backend fedcb84c. No main application activation or hosted mutation. Required JSON comparisons use null-safe assertions.

- Exact state/page/Ready agreement and changed-content negatives pass. The latest reconciled frontend suite passes400/400; the backend suite passes397/400, with only the three previously recorded incomplete public-contract/wiring tests failing. All actual v2 read, recovery and original Draft runtime tests pass. This is read-component approval only; DEV-0015 remains open for public mutation and controller retention.

## STEP-047 — single evaluation of the existing normalized row payload (LOCAL EQUIVALENCE PASS)

- Requirements BP-016/038/130/131; DEV-0016. The wider read suite passed28 of29 but the public summary again timed out in a sorted read. Do not classify STEP-046 as a complete performance pass. Instrumented full-fixture reads show642 payload builds per summary; payload self-time accounts for most of the remaining work. Merely forcing generic planning does not remove it.
- A rollback-only experiment preserves every existing field expression and adds a constant OFFSET0 barrier to the single-row base JSON subquery, so that subsequent field reads use one value. This is not candidate/list offset pagination. All214 actual fixture payloads compare exactly with the old helper. Six instrumented summaries fall from1.20–1.44 seconds to0.50–0.56 seconds in these samples. This is a local mechanism comparison, not proof of the intermittent timeout's only cause or a hosted/browser speed claim.
- Before adopting it: source parity must compare the complete original normalization after removing only this exact barrier; a broad null/boolean/post-Draft payload matrix must be byte-equal; unchanged semantic/Ready/Blocked/Draft runtime checks and full sorted reads must pass. No SQL financial expression, timeout, database/role configuration or old owner may change.
- The temporary external PG17 psql client path disappeared during this step. The already-installed PostgreSQL18 psql client now talks to the SAME disposable PostgreSQL17.6 server/database on loopback55441. No server/runtime/resource change or download was made; every SQL test retains its exact database/server-version check.

- Source parity and3504 complete normalization cases plus a null composite pass. The wider v2 run has395 passes and3 incomplete initial contract/source-wiring failures, with no runtime timeout in that run. However a subsequent repeated full-fixture sample STILL times out. Therefore this payload improvement is not recorded as resolving the full timing fault.

## STEP-048 — prevent repeated evaluation of completed eligibility (LOCAL EQUIVALENCE PASS)

- Requirements BP-016/038/130/131; DEV-0017. Exact repeated-fixture instrumentation now reproduces1,247,802 effective-section calls during the failed sample. The payload change remains fast; the expensive work is inside the existing certified eligibility statement when its final ID set is joined back to physical rows. The earlier parameter-plan wrapper alone is not a complete fence.
- A rollback-only constant MATERIALIZED annotation on that final eligible set retains the complete original predicate and prevents repeated evaluation during that final join. The experiment completes eleven exact public summary/navigation replies, with six sorted reads449–561ms. No field, eligibility rule, financial equation or timeout is removed/changed. The annotation is not a new eligibility owner.
- Before adoption: failing-first source guard; exact full predicate comparison after removing ONLY the annotation; old/new full-row equality before/after real candidate selection and post-Draft overlay; forced-join-plan linear section-check proof; repeated full fixture and affected wider regressions. Record the complete result before approving dependent selection-response work.

- The source guard failed first as expected. The runtime fixture initially attempted to demote an already-Blocked recovery; it was corrected to exercise actual promotion followed by demotion, without weakening the movement assertion. Eight focused checks pass, including full old/new rows and forced nested-loop check bounds. Ten complete public fixtures then pass consecutively;60 sorted reads442.540–617.649ms. Latest merged-source backend v2 run is397 passes/3 known unfinished initial-contract failures/0 skips in117.04s, with no runtime timeout; frontend400/400. The incomplete source-wiring tests remain release blockers, not suppressed failures. Self-review approves only the two proved evaluation mechanisms; no whole-system or hosted/browser performance claim. EVIDENCE-0062/0063.

## STEP-049 — preserve the newest sealed Office baseline (IN PROGRESS)

- Fresh remote heads are backend11b82632d7128c5bf7de14b3021bbf203fc2fb97 and frontend2527331eeba5ef8537757a4f74c52d68ae790398. The Office task reports its corrected protected release and real no-financial-scope pay-method round trip sealed; no shared publication remains in flight.
- Fast-forward only after the current read regression finishes. Preserve all owned untracked Banking files and the exact backend import/router patch. No tracked frontend Banking changes exist yet. The incoming backend paths do not overlap that patch. Re-read any instruction delta and rerun the original Draft/source-parity and new-modal tests against the merged source.
- Do not treat the reported hosted release as Banking installed-state proof. Local disposable NEW replay still requires the complete final source; no push, hosted SQL, deployment, drain or payment action belongs to this reconciliation step.

- Both fast-forwards completed. Backend router SHA256 stayed FBA239EFD48F205D0FFBAB9A4671025E90D88A2AD834787855D884A5ED6D32D4; all116 backend and41 frontend untracked files remain. No AGENTS delta was present. The current frontend's400 modal unit checks pass with no skips. The full backend v2 suite is being rerun on the reconciled source; local installed database still requires a final clean replay.

- The complete v2 rerun on both newest source heads is397/400 backend and400/400 frontend with no skips. The same three unfinished public-contract/wiring assertions remain; all runtime and original Draft-owner tests pass. Source integrity is202 migrations/456 repeatables. This closes source reconciliation, not database-installation/release proof.

## STEP-050 — bind presentation preimage to the existing candidate receipt (LOCAL COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033–035/038/130–132, DEV-0015. A later public candidate response needs the complete before-view evidence and replay identity. Source inspection proves that a second progress_json UPDATE would trigger another existing Workbench change notification. Do not add that extra update or change the trigger.
- Add optional strictly typed presentation metadata only to the unpublished v2 candidate intent branch: the previously displayed complete view digest and the already-open child read position. Under the existing session→scope→row locks, compare the digest before any payment-selection write. Store the compact before-state evidence within the existing result/receipt in its existing single session UPDATE; no second revision, audit, notification, selection owner or new table.
- Existing intents without this optional metadata and every old public selection branch must remain unchanged. The complete intent fingerprint must also bind the presentation metadata, so replay cannot alter the expected preimage or child position. A genuine no-op still writes nothing.
- Failing-first proof: stale preimage leaves all rows/session/audit/change sequence untouched; valid preimage preserves one revision/audit and the existing notification count; replay is byte-identical and write-free; changed metadata with the same request ID rejects; no-op and malformed metadata do not write. Existing candidate/global recovery/Draft tests remain required. No public route or UI is activated in this step. Full response assembly, same-revision child replacement, size limits and frontend retention remain subsequent proof gates.
- The first runtime assertion exposed a fixture assumption: the existing recovery owner already emits a notification before the existing final session update, so this fixture changes its sequence twice. The test now first executes the original intent in a rolled-back subtransaction and requires the metadata-enabled intent to produce exactly the same notification delta. No recovery/trigger behavior or revision/audit rule is changed to force a count of one.

- Six focused checks and32 combined actual candidate/global/recovery/movement/original-Draft checks pass without skips. Optional metadata rejects stale views before selection writes, binds replay to the same digest/open-child request, and retains the exact original notification delta. No-op and duplicate outcomes write nothing. Original request branches and financial formulas remain unchanged. Self-review approves this internal receipt component only; no public mutation response or UI activation is implied.

## STEP-051 — bounded candidate-response assembly (LOCAL COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033–035/038/040/077/130–132; DEV-0007/0014/0015. Implement an ungranted private prototype in the already-created2313 repeatable before exposing the planned public RPC. It calls the original selection entry once, consumes STEP-050's retained before-state, rereads complete current state, and composes the existing public summary's global/Draft/issue scalars. No new arithmetic or second session update.
- A same-revision complete Ready page is returned only when already open, using its separately typed Ready anchor. Main-only32KB, Ready512KB and combined544KB limits are checked inside the SQL operation, so oversized or inconsistent output rolls back the selection rather than returning a partial result. Full movement receipts stay on the server; the response uses the already-proved explicit compact/full invalidation format.
- Before allowing older main rows to remain, return the old view digest and exact complete other-candidate/membership comparisons. This prototype does not implement browser retention or permit it without those proofs. Tests first: same-revision all-page totals and open child; both validators on actual SQL output; exact replay/no-op; bad preimage/anchor rejection; size-failure rollback; one owner call and original Draft contract. Public routing remains absent and feature off until proof passes.

- Four focused prototype tests and36 combined selection/preimage/recovery/global/movement/eligibility/original-Draft tests pass with no skips (62.83s for the combined run). Actual100-row child and compact candidate/headline response pass both saved boundaries. Stale view, wrong anchor and oversized UTF8 response fail with all selection effects rolled back. Recovery promotion and demotion to an absent candidate return the complete current state; replay/no-op remain write-free. Self-review approves this private component only. Public request fencing, strict retention validation and browser adoption remain dependent work; no deployment or activation.

## STEP-052 — service-only candidate RPC and closed transport proof (LOCAL COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033–035/038/040/077/130–132; DEV-0007/0014/0015. Current source heads remain backend11b82632 and frontend2527331e; owned files are preserved. Add only the thin service-role public adapter over the proved private response helper, without changing the existing selection/Draft owners or four-field options object.
- Require a separate exact expected_view_digest on candidate mutation requests; return a current view digest and a closed retention proof whose before digest matches the submitted view. Missing, malformed, mismatched or extra proof fields must never permit retained rows. The Worker and browser must enforce this independently. Known SQL rollback errors stay typed rejections; malformed/lost post-mutation responses remain uncertain and require read-back, not retry.
- Failing-first tests: service-only ACL/signature; one owner call; request closed keys and digest; exact six-field retention; valid SQL output through both boundaries; old/new-view mismatch; malformed/oversized output; safe SQL failure mapping; unchanged main/read filters and original Draft contract. Planned files:2313 response repeatable; dedicated boundary tests; Worker module; frontend transport/mutation validator and their fixtures. Capability remains off. No main-row retention or public activation is implied by this step.

- The new boundary suite first failed16 of23 checks as expected. It now passes23/23. The expanded204-check run passes without skips, including actual service-role execution, browser-role/private-helper denial, exact replay/no-op, rollback errors, complete100-row output, actual movement and original Draft parity. All400 frontend checks pass. The same four-field options object is preserved; expected view evidence is a separate argument. Self-review approves this local API component only. The adapter is installed only in the disposable PG17 database; hosted activation/deployment and frontend retention are not implied.

## STEP-053 — proof-based main-row retention and dispatched request context (LOCAL COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033–035/038/077/130–132; DEV-0013/0015. Candidate requests must be built from the last accepted snapshot at dispatch, not from an earlier queued click. Include the accepted view digest and only the already-open candidate Ready page anchor. No payment IDs or amounts are submitted.
- Retain main rows only when the complete before-view digest matches, every other candidate's content and membership are proved unchanged, and the active sort value is unchanged. Replace the target row with its complete server response; reuse other row objects unmodified. Otherwise read exactly one current anchored summary, with matching view digest, global counts and candidate facts. Neither route may sum amounts or publish before complete validation.
- A freshly opened child may renew only child_revision and selected_timesheet_scope_token when every other field and the full facts_digest match. Original compatibility aliases within the same Ready response must still match exactly. Selected Timesheet token renewal must continue using the existing current-request/content-digest proof, not a new broad candidate search.
- Failing-first tests cover each sort/membership/other-candidate proof, off-page targets, full100-row cardinality, stale requests, no-op, all changed content fields, metadata-only renewal, queued intents and one terminal adoption. Actual main DOM/action wiring remains a subsequent gate. No product/copy/financial change or activation is authorised by this component.

- The26 new retention tests failed first, then passed. Additional failing-first checks caught the missing stale-view read-back and dispatch-time view binding; both are now handled. Worker and frontend reject truncated or falsely final100-row candidate pages. One older off-page synthetic test was corrected to use a genuine complete100-row page rather than one row out of two; metadata-only renewal has a positive test while changed content and same-response stale aliases remain rejected.
- All429 frontend tests and186 Worker/transport checks pass with no skips. The26-test actual PG17 run also passes: complete public selection output feeds the actual frontend reconciliation, with zero summary rereads for unchanged Candidate order/no-op/promotion and exactly one for candidate departure; original Draft and selected-only token/anchor proofs pass. No stale financial row is relabelled. Self-review approves this bounded component, not final DOM/action integration or deployed performance.

## STEP-054 — exact split-source contract guard and current regression (LOCAL SOURCE/REGRESSION PASS)

- Requirements BP-001–014/016–038/077/130–136. The initial three source tests assumed all public definitions, locks and classifier helpers would live inline in1159. The implemented additive design instead composes separately tested files. Their existing failures are the pre-change characterization, not failures to suppress.
- Replace only the inaccurate inline-location assumptions with an explicit closed source inventory and exact public→private response→original selection→private branch chain. Require all eight planned public signatures/ACLs, original classifier/recovery references, typed options guards, no forbidden SQL and no repeated selection-owner call. Keep every runtime/parity assertion. Full current v2 and frontend suites must run with local PG17 and actual frontend source enabled, no skips. No hosted release or activation belongs to this step.

- All432 current Banking Pay backend v2 checks pass with no failures or skips in132.35s, including the previously failing three source-contract tests now checking the exact split-source chain. All429 frontend v2 component tests pass. Source integrity passes202 migrations/456 repeatables. This does not include completion of the main-application structure/wiring tests, full latest-source NEW replay or deployed acceptance. The working pack manifest currently verifies30 substantive files; archive/release packaging remains separate.

## STEP-055 — preserve filtered all-page header selection through bounded v2 settlement (LOCAL COMPONENT PASS)

- Requirements BP-025–028/033/038/040/077/130–132 and existing main-header global selection. Add a thin service-only response wrapper over the already-proved modal_global_intent_v2 branch in the original selection owner. It must not loop over candidate RPCs, change any eligibility/recovery rule, or remove the old header path.
- The existing optional before-view metadata may be supplied by a global intent only with open_ready:null. Preserve the same receipt, locks, original notification behavior and no-op semantics. Global selection changes the complete filtered Ready scope, including candidates beyond100; no candidate or payment IDs come from a loaded page.
- Return a compact same-revision full-session headline/count/Draft result and complete-cache invalidation, then one anchored candidate-summary read before adoption. No old main candidate row is retained for a global mutation. No child payload is requested from the main header. This is an internal response adapter, not a new user-facing feature or selection owner; record its exact route/RPC and error envelope in17.
- Failing-first proofs: existing all-page/channel/candidate/client/hidden/Updating tests remain unchanged; wrong/missing view and global child input roll back; service-only ACL; exact original receipt/replay/no-op; one whole-scope mutation and one summary reconciliation; no amount arithmetic; strict Worker/browser inputs/outputs and original Draft parity. New SQL filenames use29 August UK time. No hosted release/activation in this step.

- All68 combined backend checks pass without skips in53.54s, including the actual106-candidate global mutation, existing channel/filter scope, public response, view-boundary and original Draft contracts. All431 frontend v2 component tests pass. The new route rejects loaded candidate/payment IDs and open-child input; every accepted header change is followed by exactly one current anchored summary read, never a visible-page update. Self-review PASS for this bounded component; original individual/group controls and full application integration remain open.

## STEP-056 — reuse the proved post-selection formatter (LOCAL COMPONENT PASS)

- Requirements BP-025–028/033/038/052/057/067/077/130–132. Before adapting individual row/group selection, extract the existing candidate response assembly into one private ungranted helper. The candidate mutation continues to call its exact existing owner once and then the same assembly, with no expression, input/output, write, recovery or Draft change.
- Pre-change characterization is the complete existing assembly text and passing candidate/global first-use suites. A new source test must fail before extraction and then prove the assembly SHA256 unchanged. Re-run actual service-role selection, full child, replay/no-op, promotion/demotion, oversized rollback and Draft tests before dependent row-adapter work. Existing heads and local-only worktrees remain unchanged; this is D0 internal code reuse, not new activation or authority.

- The new source test failed first and now proves the complete assembly SHA256 remains4257bd1a57488f4ec6a4be8d2726ae8cd328d76a5765172ffb8883a008dab112. All28 focused checks pass without skips in20.68s, including actual candidate/global selection, SQL rollback, service ACL and unchanged Draft contracts. The extracted helper is private and ungranted; the original candidate mutation still runs exactly once. Self-review PASS for this mechanical extraction only.

## STEP-057 — original individual/exact-group row-patch adapter (LOCAL COMPONENT PASS)

- Requirements BP-025–028/033/038/052/057/067/077/130–132; existing togglePreviewRow and toggleTimesheetPreviewGroup. Add one service-only bounded wrapper accepting candidate ID,1–100 exact Ready row IDs, explicit selected state, request correlation, current four options/view digest and the already-open child's page anchor. Delegate once to the original ROW_PATCH shape of pay_workbench_session_set_selected_rows. Do not route individual rows through candidate-wide selection or recreate recovery logic.
- The existing row owner advances its revision/audit even for an already-selected request and has no idempotent receipt. Preserve that behavior; request_id is correlation only. Old-revision repeats must reject and trigger current-authority read-back, never automatic mutation retry. Preserve exact existing notification, overrides, synthetic cleanup, all-channel revalidation and selected/Draft bookkeeping. New wrapper writes no extra receipt or audit.
- Reuse STEP-056 response assembly; capture complete before-view and physical movement evidence under the existing session lock. If the old owner changes another candidate, full-content proof must force summary refresh. Validate exact eligible current scope before the original call. Oversized or malformed final response must roll back the whole operation.
- Failing-first proof compares original versus wrapped row/session/audit/change-counter outcomes inside rollback savepoints, including recovery, no-op, stale, foreign/unselectable IDs and size failure. Strict Worker/browser boundaries and original Draft parity follow. Complete Timesheet/overpayment group behavior across child page boundaries remains an explicit separate integration gate; this bounded patch must not pretend a loaded partial group is the whole group. Current worktrees remain11b82632/2527331e; no hosted mutation, activation or publication.

- Four original-versus-wrapper runs pass for one row,100 rows, reselect and already-selected no-op. Every row/session/scope field, audit count and change counter agrees except the original recovery owner's precise clock_timestamp fields; those are separately proved current and monotonic rather than falsely required to equal across separate calls. Fourteen malformed/stale/foreign/nonselectable negatives and an oversized post-mutation reply leave the snapshot unchanged. Actual public replies pass Worker validation and frontend reconciliation with zero unnecessary summary reads and a complete100-row child.
- All469 backend v2 checks pass without skips in151.49s; all434 frontend v2 component checks pass. Queued individual requests capture exact IDs and use dispatch-time accepted view/child authority. Full main DOM/action wiring and whole-group behavior across child pages remain unproved, so no final BP/action parity row is marked PASS. Self-review approves only this exact bounded adapter.
- The Daily task renewed its shared TEST publication hold for two current-window routine replacements based on11b82632. Banking acknowledged no competing push/release; local work continues, with final release reconciliation still mandatory.

## STEP-058 — complete bounded direct Ready reads (LOCAL COMPONENT PASS)

- Requirements BP-033/038/040/077/130–132 and the accepted100-row/512KB child contract. Candidate mutation responses already enforce the child bound, but the direct Ready reader and some synthetic boundary fixtures lack the equivalent complete-page check. Add the same hard512KB check to the direct public read and require exact page count/position/anchors at both boundaries. Do not truncate, silently omit a row or increase a timeout.
- First prove oversized direct reads and malformed/truncated page metadata currently fail the intended tests. Then require real100-row pages, final pages, departed candidates, renewed anchors, selection responses and original Draft tests to pass unchanged. Existing child mock fixtures must become complete truthful pages, not be accepted through optional-field validation. No hosted release or capability activation.

- The new tests first failed11 backend and12 frontend cases. They now pass. Direct SQL reads and both response boundaries require a complete page with valid position/previous/anchor metadata and the same512KB UTF8 bound as selection replies. The frontend opening path reuses the selection-page validator rather than creating a second looser acceptance path. Corrected synthetic continuation fixtures now use actual page sizes/positions instead of a truncated first page described as a last page.
- All195 focused backend checks pass without skips in50.93s, including actual Ready anchor renewal, candidate/global/individual selection, oversized rollback and unchanged Draft checks. All448 frontend component checks pass. Source integrity passes202 migrations/459 repeatables. Self-review PASS for the bounded read contract only; no UI activation or hosted performance claim.
- Test-harness correction: the first new first-use test accidentally included the release file's trailing COMMIT after its synthetic fixture setup. This affected only the disposable local PG17 database. The exact two synthetic candidates, actor,110 rows, scope/session/snapshot and two trigger-created jobs/registry rows were verified and removed; no hosted call occurred. The harness now extracts only the exact function definition and rejects transaction-control statements. Fixture identities are absent after the corrected rollback test. Local sequences may advance during fixtures; final clean NEW replay remains mandatory.
- Reconciled the sealed Daily-only backend head c391f5193ec59d619d1d9dd7de740ab4a8b8e556 by fast-forward, preserving its two routine definitions/verifier/generated contract. Owned router SHA remains FBA239EFD48F205D0FFBAB9A4671025E90D88A2AD834787855D884A5ED6D32D4. New AGENTS SHA4EEAA0756B76FC109F85BB8C1FE15FCE4B150EEF3F9D0D1EBBEF209B3617B8D7 was read. The shared publication hold is released; Banking has not published. Git fetch emitted unrelated worktree-metadata cleanup permission warnings; those paths were not manually changed.

## STEP-059 — stage the main table before atomic adoption (LOCAL COMPONENT PASS)

- Requirements BP-016/017/018/025–028/033–035/038/077/130–132; contained main/child settlement and unchanged Draft snapshot. Existing main publish validates first but creates and formats DOM while applying. Add a bounded prepare/commit API so the application can stage the main table, child and compatibility aliases before one synchronous accepted-state commit. Preserve current row nodes, three sorts, exact four columns, all handlers and no optimistic values. No application activation in this component.
- Failing-first browser checks: prepare has no visible side effect; invalid staging leaves all accepted DOM untouched; an older staged commit cannot overwrite a later adoption; a commit is one-use; closing invalidates a pending commit; existing100-row node retention and mount/unmount checks remain. No new financial calculation, network operation, copy or product feature.
- Grouping audit remains open: original Timesheet/overpayment rendering groups the loaded physical rows and the original ROW_PATCH owner caps100 IDs per call. A complete group crossing a child page must not be silently treated as a complete loaded group or broadened to the whole candidate. Exact complete-group metadata/selection and grouped-action scope need a separate proof; this independent staging patch does not claim to resolve that gate.

- The synthetic in-app browser first failed because the table had no prepare API. It now passes all seven staged-adoption checks: no preparation side effects; invalid staging preserves accepted DOM; exact displayed headline; retained row node; one-use commit; stale commit rejection; closed commit rejection. Existing20 mount/unmount cycles retain one table and100 rows, accepted updates retain row nodes, and invalid replies leave the table unchanged. All448 frontend component tests pass. This approves the staging component only, not main application/alias integration or deployed performance.

## STEP-060 — preserve continuous-scope Draft safety (LOCAL COMPONENT PASS)

- Requirements BP-007/025–028/033/038/077/130–136 and23_CREATE_DRAFT_COMPATIBILITY_CONTRACT.md; original Create Draft and background-update safety. The new unpublished display helper calls the existing read-only counter owner but omits public.pay_workbench_scope_progress_v1, whose display_ready/draft_safe fields the original screen also requires. The final Draft owners have not changed and the v2 feature is off. Correct the display adapter before connecting the new screen.
- Reuse both existing authorities once; do not copy their conditions or change their functions. Preserve every counter blocker and add the exact existing scope reason when blocked. Return the exact scope booleans/reason and distinct original complete-session selection counts for later legacy adoption; never substitute filtered candidate counts. Require closed typed fields at Worker/browser boundaries and fail closed on contradictory readiness. Keep the current2KB gate bound and all total response budgets.
- Planned files: unpublished1451 helper; existing1453 rollback fixture and its missing automatic runtime runner; dedicated scope/boundary tests and truthful synthetic fixtures; Worker/browser validation and this working pack. Heads remain c391f5193ec59d619d1d9dd7de740ab4a8b8e556/2527331eeba5ef8537757a4f74c52d68ae790398 with all dirty work preserved. No hosted mutation, deployment, financial owner change or new product wording.
- Failing-first proof must cover current generation, unapplied background changes, shadow mode, active/failed upstream work, current-session work, empty filtered selection and all-channel count preservation. Compare directly to both unchanged owners; prove no rows/session/jobs/settings change during reads. Repeat selection/Ready/summary and original Draft tests and measure the extra existing read. Full legacy alias adoption, grouping and main application activation remain separate gates.

- The first tests failed for missing scope/count parity;52 newly added transport negatives also failed before implementation. They now pass. The automatic rollback runtime executes the complete existing readiness-state fixture plus unapplied generation, shadow mode and upstream queued/running/failed/dead/recovered states against both unchanged owners. It also proves105 filtered rows do not replace109 original whole-session rows. No read changes session/rows/scope/jobs/settings, and no Draft is created. The1453 fixture is now included in the automatic v2 suite rather than relying on an earlier manual run.
- Full current backend v2 suite passes539/539 without skips in182.60s; frontend component suite448/448 without skips. This includes all actual original Create Draft, candidate/global/individual selection, Ready/summary and boundary checks. Two older browser/Worker synthetic Ready fixtures needed the complete page metadata already required by STEP-058; their expectations were corrected rather than weakening validation.
- On the local105-candidate fixture,10 added scope-owner reads take0.450–5.626ms; the composed gate takes3.028–8.113ms and408bytes. Three complete navigation fixtures pass with18 sorted reads482.082–685.216ms and no timeout increase. These are local component measurements, not paired whole-application/deployed performance acceptance; earlier timing variance and all representative SLA gates remain open. Self-review approves the safety adapter only. Exact original Draft/financial owners remain unchanged; feature remains off.

## Current remaining publication gates

Actual main mounting/progress/compatibility-alias and original Draft-button integration; complete Timesheet/overpayment groups across child pages; all45 legacy actions in their new visible locations; complete Action/Updating/Blocked controls and list-only search/sort in the real application; deployed all-page navigation and selection settlement; full lifecycle/amount/frozen-Draft equivalence; representative database/Worker/browser timing and memory; latest-head clean NEW and UPGRADE contract/ACL/release-test coverage; both separately reported baseline CI fixtures; final version-controlled Bible/archive integrity; protected TEST release and deployed-asset Playwright acceptance. Component envelope/receipt/paging tests already passing above do not replace these end-to-end gates.

## STEP-061 — identify complete existing payment groups before paging (LOCAL READ COMPONENT PASS)

- Requirements BP-052/BP-057/BP-067 and exact legacy Timesheet/overpayment grouping. The existing child renderer groups only the physical lines it receives, while a child page is bounded to100. Add one private ungranted read projection that assigns the unchanged legacy group identity before paging and can recover the complete selectable group membership. Do not calculate an amount, change selection or make a loaded page authoritative for a complete group.
- The exact old getReadyTimesheetGroupKey and getOverpaymentRecoveryPresentationGroupKey declarations were added to the frozen source oracle mechanically. A SQL projection copies their JavaScript truthy/String identity semantics, composes the existing certified eligible/selection/source/filter owners and remains candidate-scoped. Failing-first proof covers every old field alias, whitespace precedence, non-overpayment negatives, a107-member group crossing the100-row page boundary and a104-row nonselectable extension that must not be included.
- All three source/oracle/actual PG17 checks pass. The107 selected selectable members retain the one exact Timesheet group key and are read without a selection/session change; the104 intentionally incomplete synthetic contracts remain excluded. No amount/recovery/Draft source occurs in the new file. This approves complete group identification only; grouped mutation, full-group display facts/actions, public envelope and real child wiring remain open.

## STEP-062 — complete group facts and atomic complete-group selection (LOCAL COMPONENT PASS)

- Requirements BP-025–028/033/038/052/057/067/077/130–132 and the complete existing Timesheet/overpayment controls. Extend each Ready row with one closed group-fact tuple computed from the complete eligible group before paging: kind, key, member count, selected count, NONE/SOME/ALL, complete display amount and complete selected display amount. Ungrouped rows carry the explicit null/zero tuple.
- A group tick sends one explicit desired state with candidate/kind/key/current revision/view evidence. It does not fan out loaded row IDs or loop over pages. The server locks and re-proves complete current membership, changes it through the existing row-selection/recovery owner once, then returns the same candidate/global/movement/Draft response authority used by other selection paths.
- The existing grouped renderer remains the presentation owner. The child presenter removes any loaded-row-ID attribute and binds its checkbox/amount only to the complete server group facts. Malformed, partial, conflicting or oversized group facts fail before presentation or write.
- Local PG17 proofs cover a107-member Timesheet group across two child pages and the existing overpayment group, including SELECT/CLEAR, replay/no-op/stale/missing/nonselectable/oversized rollback, recovery revalidation and exact original Draft compatibility. Browser/Worker boundaries accept only the closed group identity and no loaded IDs. This is local component evidence; deployed action settlement remains open.

## STEP-063 — contained application mount, original Create Draft bridge and Chromium lifecycle (LOCAL APPLICATION PASS)

- Requirements BP-001–038/052/057/067/077/080–104/110–124/130–136 and the Create Draft supplement. Insert one capability-gated integration seam immediately before the unchanged legacy Pay return. Retain the exact existing warning/progress/filter/Draft HTML and original action names. When the v2 capability is false, unavailable or invalid, return the entire unchanged legacy screen.
- The contained controller owns summary/list/child query state and one settlement queue. Existing legacy actions are invoked only through the established delegated Banking handler. Candidate/issue child closure followed by a legacy action cannot leave a duplicate controller or handler. Main direct interactions are exactly candidate/global selection, three server sorts, paging, selected-only Timesheets and non-interactive row double-click.
- On every accepted v2 graph, stage the presenter and compatibility authority together. Clear the legacy accepted physical Ready/Blocked page markers. The original Create Draft click then runs its existing complete reread, selection review, Worker request and final database owner. The v2 controller does not call Create Draft, construct selected physical payment IDs or create a second Draft gate.
- Failing-first Chromium proof exposed missing existing renderer context in the integration-generated Candidate context. The correction supplies the existing escape/date/session/effective-section/rail presentation adapters only. Candidate Banking then opens with complete Ready-only rows and current actions; Action Required/Updating/Blocked remain separate.
- Current result: frontend Banking Pay769/769; focused integrated controller64/64; Chromium3/3. Chromium proves the exact four headers, mixed tick, selected-only Timesheet ID, deductions Yes, current amount, no main View/expand control, Candidate round-trip,100 non-wrapping four-cell rows, and issue-list lifecycle. Fixtures are explicitly synthetic and network-free. This is not deployed TEST acceptance and does not activate the capability.

## STEP-064 — serial local cross-layer regression and performance checkpoint (LOCAL PASS; LATEST-HEAD/DEPLOYED OPEN)

- Run every Banking v2 backend test with test concurrency one against the disposable PostgreSQL17 database. Parallel rollback fixtures are prohibited because identical synthetic keys can create lock waits that are not application latency. Do not raise a timeout or accept a successful retry as a performance fix.
- The serial run reached568/571 before identifying two stale browser→Worker Ready fixtures missing the closed group tuple and one strict legacy Ready parity comparison that had not removed the seven approved additive v2 group fields. Add the complete null tuple to ungrouped fixture rows. Preserve byte parity for every legacy field by removing only identity plus those seven new v2 fields from the comparison. The exact12 affected tests pass after correction.
- Local timings: eligible5.67–13.29 ms; selection73.41–89.80 ms; payload25.68–26.06 ms; complete members185.32–207.64 ms; candidate facts156.38–163.20 ms; full summaries544.94–680.40 ms. The complete six-sort/navigation fixture passes with read samples533.959–1,448.908 ms and no timeout. These values are disposable local evidence only.
- The isolated backend is now reconciled to exact shared TEST head `1e07bf505eb0efb2871e77bb775fb0d2225753fd`. Before publication, rebuild/replay the complete release sequence, rerun the full serial suite, export/compare the current contract/ACL catalogue and run current frontend/Chromium tests. Hosted Worker/RPC/browser/request-count/memory and real TEST data remain mandatory acceptance gates.

## STEP-065 — capability, release catalogue and clean NEW closure (LOCAL RELEASE PASS; HOSTED UPGRADE OPEN)

- Add one replacement of the existing contract-version reader. Preserve every existing field and grant, and add only `banking_pay_workbench_v2.available`, contract version 1 and surface contract `BANKING_PAY_MODAL_STRUCTURE_V2`. Availability is true only when all eleven public v2 RPC signatures are installed. Worker capability remains independently false until hosted installation is proved.
- Register `29082026_0246_banking_pay_modal_structure_v2_verification.sql` for both NEW and UPGRADE. It proves the capability, eleven public service-only RPCs, private invoker-only helpers, fixed search paths, security mode, zero browser execution, illegal conditional-expression guards and rollback-contained first use without an authority write.
- Update the two exact browser-isolation catalogue fingerprints to include only the new service surfaces: general 643→652 and Candidate-named 108→111; browser executable remains zero. Update the legacy monolith reassert closure so the current Candidate weekly-manager and Banking selection owners cannot be overwritten. Preserve the preview→finance→final-cancellation relative authority order.
- The final clean disposable PostgreSQL17 NEW release passes end to end with 202 migrations, 464 repeatables, both catalogue verifiers, every mandatory NEW verifier and canonical contract SHA `c24bd86bbe2230636ad1782fa780cdd294c4fe15f3d5a80263a8e0887e93ce6e`. Source check and both generated Banking projections pass exactly.
- A local UPGRADE rehearsal on a blank database installed the one changed reassert repeatable, then reached the older James populated-data-only fixture and stopped because the deliberately blank NEW database has no historical template row. Do not waive it. The managed agency TEST UPGRADE must pass that verifier against its intended populated dataset before capability activation.

## STEP-066 — exact PAYE Draft guard and current-source regression (LOCAL APPLICATION/REPOSITORY PASS; DEPLOYED OPEN)

- Keep the original Create Draft action and request contract untouched. The v2 stage may enable it only when the canonical server Draft gate is true AND the existing PAYE conflicting-Draft guard allows it. Carry the exact existing ready-state label/title variants through the contained shell; do not replace an override or umbrella-only warning with a generic label.
- Focused Create Draft tests prove ALL/SOME/NONE, ALL/PAYE/UMBRELLA, promoted recovery, physical Ready/Blocked reread, incomplete summary rejection and PAYE guard non-widening. Candidate summaries never become selected payment IDs or Draft input.
- On the reconciled current heads, frontend Banking tests pass 798/798 and Chromium 3/3. Backend normal tests pass 977/977. The complete 252-file backend audit passes serially with 2,261 assertions and 28 intentional skips. The first parallel attempt caused only synthetic-key lock waits; no timeout was raised and the serial result is controlling.
- Hosted performance and product acceptance remain open. Do not call the modal professionally scale-ready until the installed Miget RPC, Worker route and visible signed-in TEST modal are timed separately under real data, and request-count/payload/memory gates pass.

## STEP-067 — protected agency TEST installation with Worker capability false (INSTALLED PASS; ACTIVATION OPEN)

- Publish the additive backend and database release chain only while `BANKING_PAY_MODAL_AVAILABLE` remains false. A failed database source, security or contract gate must leave the legacy interface controlling and must not be treated as a reason to bypass verification, broaden a grant, relax an expected hash or raise a timeout.
- Protected UPGRADE `33228516311` rejected a changed historical monolith closure. The historical file was restored byte-for-byte. `33229990858` then rejected seven provider-only `plpgsql_check.*` settings. Those settings alone were removed. `33230439167` exposed that a broad replay repair was unsafe, so it was replaced by the generated exact-current authority repair in `29082026_0326_banking_pay_release_authority_repair_v1.sql`.
- The next catalogue gates correctly stopped on three existing browser-executable helper grants. A temporary diagnostic verifier reported their exact identities; local PostgreSQL17 reproduced the same catalogue and body hashes. Service-only ACL convergence was limited to `pay_timesheet_summary_pay_state_refresh_trigger`, `pay_workbench_contract_client_dirty_fanout_chunk` and `pay_workbench_enqueue_stage_continuation`. The Candidate catalogue then uniquely identified `pay_workbench_enqueue_candidate_refresh`; its browser grant was also converged without widening any other role.
- After all security catalogues passed, the final contract still rejected one body. Protected read-only export `33232066463` compared all1,143 routine definitions and found exactly one drift: `pay_workbench_enqueue_candidate_refresh` had been replayed to older hash `394bbab8…` instead of current authoritative `afff5140…`. The repair generator now restores the existing exact current body from `07082026_1017_pay_workbench_enqueue_candidate_refresh.sql`; no expected contract was edited to accept the older definition.
- The final clean local PostgreSQL17 replay returned canonical contract SHA-256 `c24bd86bbe2230636ad1782fa780cdd294c4fe15f3d5a80263a8e0887e93ce6e`. Protected managed agency TEST UPGRADE `33232190522` then succeeded at exact source `13a1df3f3a36475ad3b7d4c5fa842bbb03ed9bf7` (source job `99046814308`, APPLY job `99046867511`). Fresh Miget read-only evidence reports 202 migrations /465 repeatables, PostgreSQL17.11, exact installed v2 reader/mutation/Draft hashes and service-only ACLs. The unchanged Draft owner hash remains `5a879bc899cff1b1f08a7da0f4cfc1705ef9d3065697f22a20c9b8609ffedfb3`.
- Cloudflare Git build `6004f590-81a4-47fc-86d2-18cf49547c10` succeeded from exact commit `13a1df3f…` using branch `test` and `npx wrangler deploy --env test`. Deployment `ec9b1d87-65d6-440c-9201-1de3d5f99a85` serves version `dd89929c-bdb3-4d12-a15d-005746b3c05b` at100%. Worker capability was still false, so none of the failed or successful database releases exposed the new modal.
- EVIDENCE-0080–0088 record the complete fail-closed chronology. This step approves installed definitions and the capability-off deployment only. Capability-on Worker deployment, frontend publication, signed-in real-data Playwright, selection restoration, exact Create Draft preflight compatibility, screenshots and hosted speed/scale acceptance remain open.

## STEP-068 — separate verified Worker capability enable (DEPLOYED PASS; FRONTEND/PRODUCT ACCEPTANCE OPEN)

- Re-read exact current Worker source and fetch `origin/test` before editing. Change only `BANKING_PAY_MODAL_AVAILABLE` false→true plus its comment, and update the capability test to require both the source switch and exact installed database contract `{available:true, contract_version:1}`. Null, absent, false and wrong-version database contracts remain unavailable and make no second RPC.
- Focused Worker capability/boundary tests pass130/130. The complete normal backend suite passes977/977. No database source, financial owner, Policy X rule, Draft route, timeout, deployment configuration or unrelated Candidate/Daily/Office path changed.
- Commit `d1b0cdd1c518a0e326158eafef0fd9418611b685` was published separately. Cloudflare Git build `bf28c149-4361-4134-94e9-3c014e57ba05` succeeded from exact branch `test` with the configured `npx wrangler deploy --env test`. Deployment `753aefaf-4706-4960-b39e-da2c8f784be9` serves version `f599188d-a507-4688-a813-e340c343ac0f` at100%.
- The first raw cross-origin probe correctly returned401 because it did not carry the CloudTMS application bearer header; it is authentication proof, not a Banking failure. The ignored Playwright credentials were loaded without output and the existing login setup refreshed the saved session. A second probe using the exact authenticated application header returned HTTP200 with `banking_pay_workbench_v2.available=true`, `contract_version=1` in420.4ms. No token, cookie or response payload beyond this capability fact was printed.
- At this point the currently deployed old frontend still rendered the legacy modal because it contained no v2 module. This was intentional staged compatibility. The following steps close the remaining gates.

## STEP-061 — real deployed session-open repair

- **Failing evidence:** the first real deployed v2 session open replayed a historical `DEAD` candidate-source build with an identity rejected by `bpay_wb_jobs_build_identity_chk`.
- **Contained correction:** replaced only the exact version-one Banking refresh closure needed to preserve the existing job identity contract. No broad queue drain, guard bypass or financial owner changed.
- **Proof:** clean PostgreSQL 17 NEW replay, exact installed contract, mandatory release verifier, 989/989 backend tests, managed TEST UPGRADE `33240331818`, deployed Worker version `89c9c7b7-daea-4989-8166-22971c1c6940` and real HTTP 200 refresh in 383–963 ms.
- **Result:** PASS.

## STEP-062 — final main-table visual contract

- Main table is exactly four columns and one physical row per candidate. The checkbox remains in Include; Timesheets is a compact selected-only shortcut inside the candidate content rather than a fifth column. Deductions is `Yes` only for a currently selected payment with a deduction, otherwise `—`.
- CSS and DOM measurement prove 40 px row/cell height, 5 px vertical padding and no wrapping. The 100-row Chromium fixture proves exactly four cells per row, one line and bounded horizontal overflow.
- **Result:** PASS.

## STEP-063 — real selection, modal and issue-surface acceptance

- Real candidate CLEAR/SELECT updated candidate tri-state, headline, included-candidate count, amount, Deductions and Timesheets availability from one accepted revision. The final SELECT restored the exact pre-test state.
- Candidate Banking remained Ready-only and retained existing controls. The selected-only Timesheets action placed exactly the selected Timesheet in the existing Timesheets Summary. Action Required and Blocked opened as separate bounded surfaces. Blocked contained no indefinite snooze.
- **Result:** PASS with exact TEST restoration; no Draft/provider/payment action.

## STEP-064 — Create Draft compatibility closure

- Existing frontend action name, handler, complete physical reread, preflight, Worker route and database Draft owners remain unchanged. V2 updates only the accepted compatibility authority and never supplies candidate summaries as Draft rows.
- Current-source compatibility tests cover one, some and 105 selected payments; promoted deductions; PAYE/Umbrella and all existing filters; stale revision/session; same-count different-selection changes; concurrent authority change; individual, candidate, grouped and all-page selection; and fail-closed adoption.
- 91/91 final checks pass. Real TEST shows Create Draft disabled because two unresolved historical refresh failures remain visible. No real Draft was clicked.
- **Result:** PASS.

## STEP-065 — release, performance and documentation seal

- Backend/database/Worker, frontend Pages, App-Ready, real signed-in browser, performance and restoration gates pass. Pages workflow `33242668890` and App-Ready workflow `33242669243` both completed successfully; the latter includes the complete Candidate/Client browser regression. The working-pack validator passes 30 manifested files, 136 functionality rows, 89 message rows, 45 action rows, 22 closed deviations and 98 unique evidence rows with no hash/size/path or sensitive-marker failure.
- The final documentation-only commit is kept distinct from application commit `4c3634877c0c43d14834d34525f4cacd328ae3b2` to avoid confusing pack edits with deployed product source.

## STEP-069 — historical source-refresh terminal repair (LOCAL RELEASE PASS; HOSTED TEST OPEN)

- **Failing evidence:** the real deployed modal produced deterministic terminal source-build failures rather than slow reads. One candidate with 42 current-scope payments reached `PAY_WORKBENCH_DEPENDENCY_IDENTITY_CONFLICT`; a separate authoritative empty-scope candidate reached `PAY_WORKBENCH_CANONICAL_PUBLICATION_NOT_READY`. Each retry used the existing bounded delivery policy and eventually exhausted. The global cron path also recorded SQLSTATE `22023` because it called a session-specific discarded-blocker repair with no session ID.
- **Exact cause proof:** fresh read-only Miget queries showed retained `REVOKED` Timesheet versions being returned by both booking-family and rotation-family dependency projections; two such versions had no current candidate financial owner. A non-revoked unowned sibling still reproduces the required fail-closed conflict. Exact wrapper execution showed the old synchronizer returning its authoritative-empty result without `public_result_json` and without changing the durable build from `RECONCILING / RECONCILE_EXECUTE`; the previous nullable `<> 'object'` check did not reject the missing field.
- **Contained correction:** require `sessionId` before the global drain can call the session repair; exclude only `REVOKED` booking/rotation history from current dependency closure; and add one postgres-only empty-scope finalizer whose execute path requires the exact current locked job, build, attempt, registry generation, complete zero seals and a sealed zero-effect plan. Capture mode returns an empty effect list; execute mode advances only that build to `RECONCILED / SOURCE_PUBLISH`.
- **Negative boundaries:** current non-revoked owner conflicts still fail closed; no browser/service grant is added to the helper; no Draft, batch, provider, payment, settlement or remittance relation is written; no timeout or retry allowance is increased; and the existing public sync signature/ACL remains unchanged.
- **Executable proof:** 997/997 normal backend tests; complete legacy/v2 `.cjs`/`.mjs` suite; security/toolchain/dependency/secret/audit verification; `db:check` at 202/475; normal TEST Worker dry-run; mandatory rollback-contained verifier; fresh PostgreSQL 17.6 NEW install; and exact contract match `8df212977d88deff1b801e10b02d307c9adffff2a88722e89e6ab0dfef69a1c4`.
- **Create Draft boundary:** the existing Create Draft handler and database owners are byte-unchanged by this repair. Create Draft was not invoked. After deployment, the approved real refresh must prove the Workbench reaches current accepted authority and the existing Draft gate reflects that authority; it must not create a Draft.
- **Current status:** backend commit `d87a247b` is locally sealed. Managed agency TEST UPGRADE, installed catalogue verification, exact Worker build/deployment and real signed-in refresh/performance acceptance remain `OPEN`.

## STEP-070 — final visual policy and code trace (POLICY PASS; IMPLEMENTATION OPEN)

- **Trigger:** user review found excessive duplicate copy, button-like inner navigation, embedded/same-surface child detail and inconsistent visual mockups. The user required a deterministic written policy cross-referenced to current code before any new images or visual implementation.
- **Current-source mapping:** re-read the outer Banking entry/tabs, Pay wrapper, v2 integration stage, main table, Candidate presenter, issue list/detail presenters, legacy grouped detail renderer, Suggested Rates Review, active Draft-status banner and the complete action ledgers.
- **Controlling output:** `24_VISUAL_PRESENTATION_POLICY_AND_CODE_TRACE.md` records 61 unique VUI rules across VUI-001 through VUI-093 with current source anchors, exact presentation, forbidden reinterpretations and proof gates.
- **Key implementation boundary:** presentation owners may be remounted or styled; financial, selection, recovery, resolution, Draft and post-Draft owners remain unchanged. Candidate/Action/Blocked detail must use one child-modal host over the still-mounted parent and the same accepted Workbench graph.
- **Rate/resolution boundary:** one payment-method summary per Timesheet group; exact component identities are retained through the existing Suggested Rates grouping. `Suggested` is unresolved; `New` requires an authoritative saved state. Overall totals are omitted unless server-certified.
- **Action boundary:** all 45 action/compatibility ledger rows must have one final entry point, one exact handler/adapter, request parity, success/no-op/stale/failure settlement, no duplicate handler and legacy fallback. The 30-action fixture alone is not the complete deletion guard.
- **Further source findings:** exact Candidate/segment and Action/Blocked columns are now fixed; a full candidate page is 100; settled VUI-006 requires confirmed Draft success to hand off once to the separate Payment Batches sheet and open the exact server-returned batch; and the frozen legacy Candidate renderer's temporary local group sums are explicitly prohibited from becoming a new authority. Existing server group scalars replace them before DOM adoption. New Original/Suggested/New totals must be server-certified or omitted.
- **Mechanical validation:** 61/61 unique policy rows have complete fields and an exact line reference; 121 frontend and three backend explicit source references are valid; the controlling ledgers remain 136/136 functionality, 89/89 messages and 45/45 unique actions; no outdated separate-button statement remains.
- **Status:** policy mapping `PASS`; implementation, executable trace guard, deterministic fixture/screenshots, local regression and real TEST proof remain `OPEN`. No application code or hosted state changed in this step.

## STEP-071 — accepted visual policy implementation (LOCAL PASS; RELEASE/DEPLOYED ACCEPTANCE OPEN)

- **Implementation boundary:** remount/restyle the existing Banking controllers and presenters only. Main candidate rows remain one line; details open in the dedicated child host; outer Banking and inner Pay navigation use the accepted tab structure; Payment Batches is a separate outer sheet with normal entry collapsed and successful-Draft handoff preserved.
- **Authority boundary:** amounts, deductions, selection, eligibility, recovery, effective section, suggested/saved resolutions and Draft readiness remain server-owned. The candidate summary is never Draft input. The existing Create Draft handler, complete physical reread, preflight and server owners are unchanged and are not invoked in local acceptance.
- **Action parity:** existing Candidate, grouped payment, segment, resolution, snooze, Timesheets, export, Action Required, Blocked, filter, sort, paging, refresh, retry and Draft entry points remain connected through the original adapters. Candidate and issue detail reuse the same accepted Workbench graph and do not create a second economic/session owner.
- **Local proof:** Banking frontend 838/838; deterministic renderer and presentation-oracle checks pass; Chromium 4/4 including exactly 100 one-line rows; backend 1,005/1,005; `db:check` 202/479. The candidate amount wrapper correction is presentation-only and explicitly tested as a two-line full/selected amount.
- **Broader baseline:** 1,370/1,371 frontend JavaScript tests pass. The single failure is independently present in the current remote frontend head and concerns a stale Import Review explanatory-comment assertion, not Banking Pay. It is retained transparently rather than hidden or patched without ownership.
- **Current release gate:** do not publish on the observed backend `cf69bbd2c313a3bae2ee785483555e87dd51d953`; its protected database APPLY failed in the unrelated Daily booked-source verifier with `ACTOR_NOT_FOUND_OR_INACTIVE`. Re-fetch the exact sealed green backend head before constructing the Banking commit. Re-parse its complete generated contract and require exactly one old Banking action-page hash before replacing it with the proved new hash.
- **Remaining gates:** exact-head fast-forward publication; protected agency TEST UPGRADE; installed routine/ACL/contract proof; Worker and Pages deployment; real signed-in Banking open, tabs, sorting, paging, child modals and selected-only Timesheets proof; no-click Create Draft contract observation; deployed screenshots; hosted timing/request-count evidence; and final pack reseal.
- **Result:** `LOCAL PASS`; release and deployed real-world acceptance remain `OPEN`.
